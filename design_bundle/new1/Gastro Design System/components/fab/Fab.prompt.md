# Fab

Floating action button — one key persistent action anchored to a screen edge. Circular, elevated; extended (pill) when given a `label`.

```jsx
<Fab icon="shopping-cart-simple" label="View cart" onClick={openCart} />
```

`size` md/lg = 56/64px. Uses `--elevation-raised`.
