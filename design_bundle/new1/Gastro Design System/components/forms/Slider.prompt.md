# Slider

Draggable single-value control within a range — price ceiling, distance.

```jsx
<Slider min={0} max={50} value={maxPrice} onChange={setMaxPrice} formatValue={v => '$' + v} />
```

Accent fills the track up to the thumb.
