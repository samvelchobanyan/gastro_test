# FilterBar

Horizontal pill toggle — icon left, label right. Alternative to `Filter` (round icon-over-label) for denser filter rows.

```jsx
<FilterBar label="Italian" icon="fork-knife" selected={sel==='it'} onClick={()=>setSel('it')} />
```
