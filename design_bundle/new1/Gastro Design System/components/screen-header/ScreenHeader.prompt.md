# ScreenHeader

Top-of-screen page title in Heading 1 style, with an optional back button and right-side action slot. Full-width, sits below the status bar.

```jsx
<ScreenHeader title="Your orders" onBack={goBack} />
```

For a smaller in-page section label use `SectionHeader` (Heading 3).
