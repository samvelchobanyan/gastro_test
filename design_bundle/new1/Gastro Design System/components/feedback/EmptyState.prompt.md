# EmptyState

Shown when a list/section has no content. Icon + message + optional action.

```jsx
<EmptyState icon="shopping-cart-simple" title="Your cart is empty"
  description="Add dishes to get started." actionLabel="Browse menu" onAction={browse} />
```
