# BottomSheet

Bottom-anchored modal for focused tasks (customization, filters). Scrim + drag handle. Mount inside a `position: relative` screen frame.

```jsx
<BottomSheet open={open} onClose={close} title="Customize" footer={<Button fullWidth>Add to cart</Button>}>
  …
</BottomSheet>
```
