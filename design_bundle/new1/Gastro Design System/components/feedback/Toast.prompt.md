# Toast

Brief non-blocking confirmation, bottom-anchored. Dark surface; optional action + auto-dismiss.

```jsx
<Toast open={shown} tone="success" message="Added to cart" actionLabel="Undo" onAction={undo} />
```
