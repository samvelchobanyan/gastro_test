# DatePicker

Horizontal day-scroller for near-term dates — reservations, scheduled orders. Not a full month calendar.

```jsx
<DatePicker value={date} onChange={setDate} daysAhead={21} label="Reservation date" />
```
