# Card

Generic content container — white surface, 16px radius, subtle shadow. `interactive` adds press-scale + button semantics.

```jsx
<Card interactive onClick={open}>
  <h3>Order #1024</h3>
  <p>Delivered · $34.50</p>
</Card>
```
