# PaymentMethodPicker

List of saved payment methods + "Add payment method". Composes PaymentMethodCard.

```jsx
<PaymentMethodPicker methods={methods} selectedId={id} onSelect={setId} onAddNew={addCard} />
```
