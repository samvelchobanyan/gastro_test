# SectionHeader

Heading 2 styled label that introduces a page section, with an optional trailing text action.

```jsx
<SectionHeader title="Popular near you" action="See all" onActionClick={...} />
```
