# BuyAgainCard

Vertical product card for re-order rails: photo on top, product name (Body Regular), then location with pin (Body Small). First variant in the Product Cards group.

```jsx
<BuyAgainCard name="Margherita Pizza" location="Downtown" imageUrl={img} />
```

Compact fixed width (default 160px) for horizontal scroll rows; image is a swappable placeholder.
