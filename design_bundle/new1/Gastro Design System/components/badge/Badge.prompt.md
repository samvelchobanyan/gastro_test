# Badge

Count or status indicator. Overlays a child (icon) or renders standalone.

```jsx
<Badge count={3}><i className="ph ph-shopping-cart-simple" style={{fontSize:24}} /></Badge>
<Badge dot tone="success" />
```
