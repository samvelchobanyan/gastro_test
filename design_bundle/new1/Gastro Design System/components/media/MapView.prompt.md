# MapView

Placeholder map surface (grid background + pin) for address selection / live tracking. Swap for a real map SDK in production.

```jsx
<MapView height={220} showCourier markerLabel="Arriving in 12 min" />
```
