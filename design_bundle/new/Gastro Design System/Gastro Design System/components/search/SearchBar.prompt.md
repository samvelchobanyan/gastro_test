# SearchBar

Pill-shaped search input with magnifier, clear button and optional live suggestions.

```jsx
<SearchBar value={q} onChange={setQ} suggestions={matches} onSubmit={runSearch} />
```

Sunken fill by default; border turns accent on focus.
