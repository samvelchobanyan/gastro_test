# OrderTotalBreakdown

Itemized price summary — subtotal, delivery, fees, discount, tax, total (emphasized).

```jsx
<OrderTotalBreakdown subtotal={28} deliveryFee={2.5} discount={4} tax={2.1} />
```
