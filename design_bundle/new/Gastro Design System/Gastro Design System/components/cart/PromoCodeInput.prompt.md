# PromoCodeInput

Discount code field with apply button; swaps to a success state once applied.

```jsx
<PromoCodeInput value={code} onChange={setCode} onApply={apply}
  applied={applied} onRemove={() => setApplied(null)} error={error} />
```
