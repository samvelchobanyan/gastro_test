# Chip

Interactive tag — selectable toggle (filters) and/or removable. For a static, non-interactive label, use `Tag`.

```jsx
<Chip label="Under $15" selected={on} onClick={toggle} />
<Chip label="Spicy" onRemove={remove} />
```
