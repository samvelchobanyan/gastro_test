# RatingReviewPrompt

Post-delivery rating card — stars + optional comment. Submit disabled until a rating is chosen.

```jsx
<RatingReviewPrompt restaurantName="Nolana Pizzeria" rating={rating} onRatingChange={setRating}
  review={review} onReviewChange={setReview} onSubmit={submit} onSkip={skip} />
```
