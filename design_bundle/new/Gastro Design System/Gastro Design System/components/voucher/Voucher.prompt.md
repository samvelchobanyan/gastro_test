# Voucher

Reward/coupon list row: square product image placeholder (swappable per client), product name (Body Regular) and a remaining-count line (Body Small).

```jsx
<Voucher name="Free Coffee" remaining={5} image={img} />
```
