# RatingStars

Star rating — display (with halves + count) or interactive input.

```jsx
<RatingStars value={4.5} count={128} />
<RatingStars value={rating} readOnly={false} onChange={setRating} size={28} />
```
