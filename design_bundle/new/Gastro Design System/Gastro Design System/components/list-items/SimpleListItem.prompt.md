# SimpleListItem

Minimal navigational row — a text label and a right-pointing arrow. Use for plain menu/settings lists. For icons, subtitles or trailing values, use `AdvancedListItem`.

```jsx
<SimpleListItem label="Terms & conditions" onClick={open} />
```
