# Checkbox

Binary / multi-select control. Uses a check glyph (not color alone) for accessibility.

```jsx
<Checkbox checked={agree} onChange={setAgree} label="Save this address" />
```

`indeterminate` shows a dash for partial group selection.
