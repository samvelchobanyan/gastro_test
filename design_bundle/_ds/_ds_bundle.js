/* @ds-bundle: {"format":5,"namespace":"GastroDesignSystem_472aed","components":["Avatar","Badge","Button","CartSummaryBar","OrderTotalBreakdown","PromoCodeInput","PaymentMethodCard","PaymentMethodPicker","TipSelector","Chip","ReservationConfirmationCard","Card","Divider","Fab","BottomSheet","Dialog","EmptyState","ErrorState","ProgressBar","Skeleton","Spinner","Toast","Filter","FilterBar","Checkbox","FormGroup","Radio","RadioGroup","Slider","Stepper","Switch","IconButton","Accordion","Dropdown","TextArea","TextInput","AdvancedListItem","SimpleListItem","LoyaltyRewardsBadge","LoyaltyWidget","MapView","CartLineItem","ItemCustomizationSheet","MenuItemCard","BottomTabBar","Breadcrumb","NavigationDrawer","PageIndicator","TopAppBar","NewsListing","LiveOrderTrackingMap","OrderHistoryCard","OrderStatusTimeline","RatingReviewPrompt","ReorderButton","Calendar","DatePicker","TimePicker","QuickActionTile","QuickActionTiles","RatingStars","AddressSelector","BranchCard","DeliveryPickupToggle","DeliveryTimeEstimate","RestaurantCard","RestaurantHeader","SearchBar","SearchFiltersPanel","MenuCategoryTabs","SegmentedControl","Tag","Tooltip","SectionHeader"],"loader":"inline-jsx-babel"} */

(() => {
  const __ds_ns = (window.GastroDesignSystem_472aed = window.GastroDesignSystem_472aed || {});
  const __ds_scope = {};
  __ds_ns.__errors = __ds_ns.__errors || [];
  __ds_ns.__ready = false;

  const SOURCES = {"components/avatar/Avatar.jsx":"import React from 'react';\n\n/**\n * Avatar — circular image / initials representing a user or entity.\n * Falls back to initials, then a user glyph. Optional status dot.\n */\nexport function Avatar({\n  src = null,\n  name = '',\n  size = 'md',         // 'xs' 24 | 'sm' 32 | 'md' 40 | 'lg' 56 | 'xl' 72\n  shape = 'circle',    // 'circle' | 'rounded'\n  status = null,       // 'online' | 'busy' | null\n}) {\n  const sizes = { xs: 24, sm: 32, md: 40, lg: 56, xl: 72 };\n  const dim = sizes[size] || 40;\n  const initials = name.split(' ').map((w) => w[0]).slice(0, 2).join('').toUpperCase();\n\n  const box = {\n    position: 'relative', width: dim, height: dim, flex: '0 0 auto',\n    borderRadius: shape === 'circle' ? '50%' : 'var(--radius-md)',\n    background: src ? `center/cover no-repeat url(\"${src}\")` : 'var(--accent-100)',\n    display: 'inline-flex', alignItems: 'center', justifyContent: 'center', overflow: 'hidden',\n    color: 'var(--color-text-accent)', fontFamily: 'var(--font-family-base)',\n    fontSize: Math.round(dim * 0.38), fontWeight: 'var(--font-weight-semibold)',\n  };\n  const dot = {\n    position: 'absolute', bottom: 0, right: 0, width: Math.max(8, dim * 0.24), height: Math.max(8, dim * 0.24),\n    borderRadius: '50%', border: '2px solid var(--color-bg-main)',\n    background: status === 'busy' ? 'var(--color-status-warning)' : 'var(--color-status-success)',\n  };\n\n  return (\n    <span style={box} role=\"img\" aria-label={name || 'Avatar'}>\n      {!src && (initials || <i className=\"ph ph-user\" aria-hidden=\"true\" style={{ fontSize: Math.round(dim * 0.5) }} />)}\n      {status && <span style={dot} aria-hidden=\"true\" />}\n    </span>\n  );\n}\n","components/badge/Badge.jsx":"import React from 'react';\n\n/**\n * Badge — small numeric or status indicator. Use `dot` for presence, a\n * number for counts (e.g. cart), or `standalone` inline pill. Counts clamp\n * at `max` (default 99 -> \"99+\").\n */\nexport function Badge({\n  count = null,\n  dot = false,\n  max = 99,\n  tone = 'error',      // 'error' | 'brand' | 'success' | 'neutral'\n  children = null,     // element to overlay onto (optional)\n}) {\n  const tones = {\n    error: { bg: 'var(--color-status-error)', color: 'var(--color-text-on-brand)' },\n    brand: { bg: 'var(--color-brand-primary)', color: 'var(--color-text-on-brand)' },\n    success: { bg: 'var(--color-status-success)', color: 'var(--color-text-on-brand)' },\n    neutral: { bg: 'var(--gray-700)', color: 'var(--color-text-inverse)' },\n  };\n  const t = tones[tone] || tones.error;\n  const label = count != null && count > max ? `${max}+` : count;\n\n  const pill = {\n    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n    minWidth: dot ? 10 : 18, height: dot ? 10 : 18, padding: dot ? 0 : '0 5px',\n    boxSizing: 'border-box', background: t.bg, color: t.color,\n    borderRadius: 'var(--radius-full)', fontFamily: 'var(--font-family-base)',\n    fontSize: 11, fontWeight: 'var(--font-weight-bold)', lineHeight: 1,\n    fontVariantNumeric: 'tabular-nums',\n  };\n\n  if (!children) return <span style={pill} aria-label={dot ? 'New' : String(label)}>{!dot && label}</span>;\n\n  return (\n    <span style={{ position: 'relative', display: 'inline-flex' }}>\n      {children}\n      <span style={{ ...pill, position: 'absolute', top: -4, right: -4, border: '2px solid var(--color-bg-main)' }}>\n        {!dot && label}\n      </span>\n    </span>\n  );\n}\n","components/buttons/Button.jsx":"import React from 'react';\n\n/**\n * Button — primary CTA, secondary (outline) and tertiary (text) actions.\n * Neutral/base level; brand applied via tokens. Heights follow the spec\n * button scale (56 / 48 / 40 / 32). Supports leading/trailing icons,\n * loading (frozen size + spinner) and disabled states.\n */\nexport function Button({\n  children,\n  variant = 'primary', // 'primary' | 'secondary' | 'tertiary' | 'destructive'\n  size = 'md',         // 'lg' 56 | 'md' 48 | 'sm' 40 | 'xs' 32\n  fullWidth = false,\n  disabled = false,\n  loading = false,\n  iconLeft = null,     // phosphor name, e.g. 'shopping-cart-simple'\n  iconRight = null,\n  onClick = () => {},\n  type = 'button',\n  ariaLabel,\n}) {\n  const [pressed, setPressed] = React.useState(false);\n\n  const heights = { lg: 56, md: 48, sm: 40, xs: 32 };\n  const fonts = { lg: 16, md: 16, sm: 14, xs: 13 };\n  const pads = { lg: 24, md: 20, sm: 16, xs: 12 };\n  const h = heights[size];\n  const radius = size === 'xs' ? 'var(--radius-sm)' : 'var(--radius-md)';\n\n  const palettes = {\n    primary: {\n      bg: pressed ? 'var(--button-primary-bg-active)' : 'var(--button-primary-bg)',\n      color: 'var(--button-primary-text)', border: 'transparent',\n    },\n    secondary: {\n      bg: pressed ? 'var(--button-secondary-bg-active)' : 'var(--button-secondary-bg)',\n      color: 'var(--button-secondary-text)', border: 'var(--button-secondary-border)',\n    },\n    tertiary: {\n      bg: pressed ? 'var(--button-tertiary-bg-active)' : 'transparent',\n      color: 'var(--button-tertiary-text)', border: 'transparent',\n    },\n    destructive: {\n      bg: pressed ? 'var(--red-600)' : 'var(--color-status-error)',\n      color: 'var(--color-text-on-brand)', border: 'transparent',\n    },\n  };\n  const p = palettes[variant] || palettes.primary;\n\n  const style = {\n    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n    gap: 'var(--space-sm)', width: fullWidth ? '100%' : 'auto',\n    height: h, padding: `0 ${pads[size]}px`, boxSizing: 'border-box',\n    background: p.bg, color: p.color,\n    border: `var(--border-width-default) solid ${p.border}`,\n    borderRadius: radius,\n    fontFamily: 'var(--font-family-base)', fontSize: fonts[size],\n    fontWeight: 'var(--font-weight-semibold)', lineHeight: 1,\n    cursor: disabled || loading ? 'not-allowed' : 'pointer',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    transition: 'var(--transition-press)',\n    WebkitTapHighlightColor: 'transparent', position: 'relative', whiteSpace: 'nowrap',\n  };\n\n  const clear = () => setPressed(false);\n\n  return (\n    <button\n      type={type} style={style} disabled={disabled || loading}\n      onClick={onClick} aria-label={ariaLabel} aria-busy={loading || undefined}\n      onPointerDown={() => !disabled && !loading && setPressed(true)}\n      onPointerUp={clear} onPointerLeave={clear}\n    >\n      {loading && (\n        <span\n          aria-hidden=\"true\"\n          style={{\n            width: 18, height: 18, borderRadius: '50%',\n            border: '2px solid currentColor', borderTopColor: 'transparent',\n            opacity: 0.9, animation: 'gastro-spin .7s linear infinite',\n          }}\n        />\n      )}\n      {!loading && iconLeft && <i className={`ph ph-${iconLeft}`} aria-hidden=\"true\" style={{ fontSize: 20 }} />}\n      {!loading && <span>{children}</span>}\n      {!loading && iconRight && <i className={`ph ph-${iconRight}`} aria-hidden=\"true\" style={{ fontSize: 20 }} />}\n      <style>{'@keyframes gastro-spin{to{transform:rotate(360deg)}}'}</style>\n    </button>\n  );\n}\n","components/cart/CartSummaryBar.jsx":"import React from 'react';\n\n/**\n * CartSummaryBar — persistent bottom bar showing item count + total,\n * tappable to open the full cart. Sits above the bottom tab bar or\n * screen edge; elevated like a FAB.\n */\nexport function CartSummaryBar({ itemCount = 0, total = 0, onOpen = () => {} }) {\n  if (itemCount <= 0) return null;\n  const [pressed, setPressed] = React.useState(false);\n  return (\n    <button type=\"button\" onClick={onOpen}\n      onPointerDown={() => setPressed(true)} onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n      style={{\n        display: 'flex', alignItems: 'center', justifyContent: 'space-between', width: '100%',\n        minHeight: 56, padding: '0 var(--space-base)', boxSizing: 'border-box', border: 'none',\n        borderRadius: 'var(--radius-md)', cursor: 'pointer', fontFamily: 'var(--font-family-base)',\n        background: pressed ? 'var(--button-primary-bg-active)' : 'var(--button-primary-bg)',\n        color: 'var(--button-primary-text)', boxShadow: 'var(--elevation-raised)',\n        transition: 'var(--transition-press)', WebkitTapHighlightColor: 'transparent',\n      }}>\n      <span style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)' }}>\n        <span style={{\n          display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: 24, height: 24,\n          borderRadius: 'var(--radius-full)', background: 'var(--alpha-white-24)', fontSize: 12, fontWeight: 'var(--font-weight-bold)',\n        }}>{itemCount}</span>\n        <i className=\"ph ph-shopping-cart-simple\" aria-hidden=\"true\" style={{ fontSize: 'var(--icon-md)' }} />\n        <span style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)' }}>View cart</span>\n      </span>\n      <span style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-bold)', fontVariantNumeric: 'tabular-nums' }}>\n        ${total.toFixed(2)}\n      </span>\n    </button>\n  );\n}\n","components/cart/OrderTotalBreakdown.jsx":"import React from 'react';\n\n/**\n * OrderTotalBreakdown — itemized price summary (subtotal, delivery, fees,\n * discount, tax, total). Total row is visually emphasized.\n */\nexport function OrderTotalBreakdown({\n  subtotal = 0,\n  deliveryFee = 0,\n  serviceFee = 0,\n  discount = 0,\n  tax = 0,\n}) {\n  const total = subtotal + deliveryFee + serviceFee + tax - discount;\n  const row = (label, value, opts = {}) => (\n    <div style={{ display: 'flex', justifyContent: 'space-between', padding: '4px 0' }}>\n      <span style={{ fontSize: 'var(--font-size-body)', color: opts.muted ? 'var(--color-text-tertiary)' : 'var(--color-text-secondary)' }}>{label}</span>\n      <span style={{\n        fontSize: 'var(--font-size-body)', fontVariantNumeric: 'tabular-nums',\n        color: opts.negative ? 'var(--color-status-success)' : 'var(--color-text-primary)',\n      }}>{opts.negative ? '−' : ''}${Math.abs(value).toFixed(2)}</span>\n    </div>\n  );\n\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      {row('Subtotal', subtotal)}\n      {row(deliveryFee ? 'Delivery fee' : 'Delivery', deliveryFee, { muted: deliveryFee === 0 })}\n      {serviceFee > 0 && row('Service fee', serviceFee)}\n      {discount > 0 && row('Discount', discount, { negative: true })}\n      {row('Tax', tax, { muted: true })}\n      <div style={{\n        display: 'flex', justifyContent: 'space-between', marginTop: 'var(--space-sm)',\n        paddingTop: 'var(--space-md)', borderTop: 'var(--border-width-default) solid var(--color-border-muted)',\n      }}>\n        <span style={{ fontSize: 'var(--font-size-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>Total</span>\n        <span style={{ fontSize: 'var(--font-size-h3)', fontWeight: 'var(--font-weight-bold)', color: 'var(--color-text-primary)', fontVariantNumeric: 'tabular-nums' }}>${total.toFixed(2)}</span>\n      </div>\n    </div>\n  );\n}\n","components/cart/PromoCodeInput.jsx":"import React from 'react';\n\n/**\n * PromoCodeInput — inline field + apply button for discount codes.\n * Shows an applied state with the discount amount and a remove action.\n */\nexport function PromoCodeInput({\n  value = '',\n  onChange = () => {},\n  onApply = () => {},\n  applied = null,        // { code, amount } or null\n  onRemove = () => {},\n  error = null,\n}) {\n  if (applied) {\n    return (\n      <div style={{\n        display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 'var(--space-md)',\n        padding: 'var(--space-md)', background: 'var(--color-bg-success)', borderRadius: 'var(--radius-md)',\n        fontFamily: 'var(--font-family-base)',\n      }}>\n        <span style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', color: 'var(--color-text-success)' }}>\n          <i className=\"ph-fill ph-check-circle\" aria-hidden=\"true\" style={{ fontSize: 20 }} />\n          <span style={{ fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)' }}>{applied.code} applied · −${applied.amount.toFixed(2)}</span>\n        </span>\n        <button type=\"button\" onClick={onRemove} style={{ border: 'none', background: 'transparent', color: 'var(--color-text-success)', cursor: 'pointer', fontSize: 18 }} aria-label=\"Remove promo code\">\n          <i className=\"ph ph-x\" aria-hidden=\"true\" />\n        </button>\n      </div>\n    );\n  }\n\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      <div style={{ display: 'flex', gap: 'var(--space-sm)' }}>\n        <input\n          value={value} onChange={(e) => onChange(e.target.value.toUpperCase())} placeholder=\"Promo code\"\n          style={{\n            flex: 1, height: 48, padding: '0 var(--input-padding-x)', boxSizing: 'border-box',\n            border: `var(--border-width-default) solid ${error ? 'var(--input-border-error)' : 'var(--input-border)'}`,\n            borderRadius: 'var(--radius-input)', background: 'var(--input-bg)', color: 'var(--input-text)',\n            fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)', outline: 'none', textTransform: 'uppercase',\n          }}\n        />\n        <button type=\"button\" onClick={() => onApply(value)} disabled={!value} style={{\n          height: 48, padding: '0 var(--space-lg)', borderRadius: 'var(--radius-md)',\n          background: 'var(--button-secondary-bg)', border: 'var(--border-width-default) solid var(--button-secondary-border)',\n          color: 'var(--button-secondary-text)', fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)',\n          fontWeight: 'var(--font-weight-semibold)', cursor: value ? 'pointer' : 'not-allowed',\n          opacity: value ? 1 : 'var(--state-disabled-opacity)',\n        }}>Apply</button>\n      </div>\n      {error && (\n        <div style={{ marginTop: 'var(--space-sm)', display: 'flex', alignItems: 'center', gap: 'var(--space-xs)', fontSize: 'var(--font-size-caption)', color: 'var(--color-text-error)' }}>\n          <i className=\"ph ph-warning-circle\" aria-hidden=\"true\" style={{ fontSize: 14 }} />{error}\n        </div>\n      )}\n    </div>\n  );\n}\n","components/checkout/PaymentMethodCard.jsx":"import React from 'react';\n\n/**\n * PaymentMethodCard — single payment method row (card, wallet) with brand\n * icon, masked number and a selected/radio indicator. Use inside\n * PaymentMethodPicker or standalone in a settings list.\n */\nexport function PaymentMethodCard({\n  type = 'card',        // 'card' | 'apple-pay' | 'google-pay' | 'cash'\n  last4 = null,\n  label = null,\n  selected = false,\n  onClick = () => {},\n}) {\n  const icons = { card: 'credit-card', 'apple-pay': 'apple-logo', 'google-pay': 'google-logo', cash: 'money' };\n  const title = label || (last4 ? `•••• ${last4}` : type === 'apple-pay' ? 'Apple Pay' : type === 'google-pay' ? 'Google Pay' : 'Cash');\n\n  return (\n    <button type=\"button\" onClick={onClick} style={{\n      display: 'flex', alignItems: 'center', gap: 'var(--space-md)', width: '100%', minHeight: 56,\n      padding: '0 var(--space-md)', border: `var(--border-width-default) solid ${selected ? 'var(--color-brand-primary)' : 'var(--color-border-default)'}`,\n      borderRadius: 'var(--radius-md)', background: selected ? 'var(--color-brand-subtle)' : 'var(--color-bg-main)',\n      cursor: 'pointer', textAlign: 'left', fontFamily: 'var(--font-family-base)', WebkitTapHighlightColor: 'transparent',\n      transition: 'var(--transition-colors)',\n    }}>\n      <span style={{\n        width: 40, height: 28, borderRadius: 'var(--radius-xs)', background: 'var(--color-bg-surface-sunken)',\n        display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--color-text-secondary)', flex: '0 0 auto',\n      }}><i className={`ph ph-${icons[type] || 'credit-card'}`} aria-hidden=\"true\" style={{ fontSize: 18 }} /></span>\n      <span style={{ flex: 1, fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-medium)', color: 'var(--color-text-primary)' }}>{title}</span>\n      <span aria-hidden=\"true\" style={{\n        width: 20, height: 20, borderRadius: '50%', flex: '0 0 auto',\n        border: `2px solid ${selected ? 'var(--color-brand-primary)' : 'var(--color-border-strong)'}`,\n        display: 'flex', alignItems: 'center', justifyContent: 'center',\n      }}>{selected && <span style={{ width: 10, height: 10, borderRadius: '50%', background: 'var(--color-brand-primary)' }} />}</span>\n    </button>\n  );\n}\n","components/checkout/PaymentMethodPicker.jsx":"import React from 'react';\nimport { PaymentMethodCard } from './PaymentMethodCard.jsx';\n\n/**\n * PaymentMethodPicker — vertical list of PaymentMethodCards with an\n * \"Add payment method\" action. Manages the selected id.\n */\nexport function PaymentMethodPicker({ methods = [], selectedId = null, onSelect = () => {}, onAddNew = () => {} }) {\n  return (\n    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-sm)', fontFamily: 'var(--font-family-base)' }}>\n      {methods.map((m) => (\n        <PaymentMethodCard key={m.id} type={m.type} last4={m.last4} label={m.label} selected={m.id === selectedId} onClick={() => onSelect(m.id)} />\n      ))}\n      <button type=\"button\" onClick={onAddNew} style={{\n        display: 'flex', alignItems: 'center', gap: 'var(--space-md)', minHeight: 56, padding: '0 var(--space-md)',\n        border: 'var(--border-width-default) dashed var(--color-border-default)', borderRadius: 'var(--radius-md)',\n        background: 'transparent', color: 'var(--color-text-accent)', cursor: 'pointer', textAlign: 'left',\n        fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)', WebkitTapHighlightColor: 'transparent',\n      }}>\n        <i className=\"ph ph-plus-circle\" aria-hidden=\"true\" style={{ fontSize: 20 }} />Add payment method\n      </button>\n    </div>\n  );\n}\n","components/checkout/TipSelector.jsx":"import React from 'react';\n\n/**\n * TipSelector — segmented preset percentages + custom amount for tipping\n * the courier/staff. Shows the computed dollar amount per preset.\n */\nexport function TipSelector({ subtotal = 0, presets = [10, 15, 20], value = 15, onChange = () => {}, allowCustom = true }) {\n  const isCustom = !presets.includes(value) && value !== 0;\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      <div style={{ display: 'flex', gap: 8 }}>\n        {presets.map((p) => {\n          const active = value === p;\n          return (\n            <button key={p} type=\"button\" onClick={() => onChange(p)} style={{\n              flex: 1, height: 56, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',\n              border: active ? 'none' : 'var(--border-width-default) solid var(--color-border-default)',\n              borderRadius: 'var(--radius-md)', background: active ? 'var(--color-brand-primary)' : 'var(--color-bg-main)',\n              color: active ? 'var(--color-text-on-brand)' : 'var(--color-text-primary)', cursor: 'pointer',\n              WebkitTapHighlightColor: 'transparent', transition: 'var(--transition-colors)',\n            }}>\n              <span style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-bold)' }}>{p}%</span>\n              <span style={{ fontSize: 11, opacity: 0.85 }}>${(subtotal * p / 100).toFixed(2)}</span>\n            </button>\n          );\n        })}\n        <button type=\"button\" onClick={() => onChange(0)} style={{\n          flex: 1, height: 56, border: value === 0 ? 'none' : 'var(--border-width-default) solid var(--color-border-default)',\n          borderRadius: 'var(--radius-md)', background: value === 0 ? 'var(--color-brand-primary)' : 'var(--color-bg-main)',\n          color: value === 0 ? 'var(--color-text-on-brand)' : 'var(--color-text-primary)', cursor: 'pointer',\n          fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)', WebkitTapHighlightColor: 'transparent',\n        }}>No tip</button>\n      </div>\n      {allowCustom && (\n        <button type=\"button\" onClick={() => onChange(isCustom ? value : 25)} style={{\n          marginTop: 'var(--space-sm)', border: 'none', background: 'transparent', color: 'var(--color-text-accent)',\n          fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)', cursor: 'pointer', padding: 0,\n        }}>Custom amount</button>\n      )}\n    </div>\n  );\n}\n","components/chip/Chip.jsx":"import React from 'react';\n\n/**\n * Chip — interactive tag: a selectable toggle (filter chips) and/or\n * removable (with an x). For a static, non-interactive label, use Tag.\n */\nexport function Chip({\n  label = '',\n  icon = null,          // phosphor name\n  selected = false,\n  onClick = null,\n  onRemove = null,\n  size = 'md',          // 'sm' 24 | 'md' 32\n}) {\n  const h = size === 'sm' ? 24 : 32;\n  const t = selected\n    ? { bg: 'var(--chip-bg-selected)', color: 'var(--chip-text-selected)' }\n    : { bg: 'var(--chip-bg)', color: 'var(--chip-text)' };\n  const interactive = Boolean(onClick);\n\n  const style = {\n    display: 'inline-flex', alignItems: 'center', gap: 6, height: h,\n    padding: `0 ${size === 'sm' ? 10 : 12}px`, boxSizing: 'border-box',\n    background: t.bg, color: t.color, border: 'none', borderRadius: 'var(--radius-full)',\n    fontFamily: 'var(--font-family-base)', fontSize: size === 'sm' ? 12 : 'var(--font-size-body)',\n    fontWeight: 'var(--font-weight-semibold)', cursor: interactive ? 'pointer' : 'default',\n    WebkitTapHighlightColor: 'transparent', whiteSpace: 'nowrap',\n    transition: 'var(--transition-colors)',\n  };\n  const Tag = interactive ? 'button' : 'span';\n\n  return (\n    <Tag type={interactive ? 'button' : undefined} style={style} onClick={onClick} aria-pressed={interactive ? selected : undefined}>\n      {selected && <i className=\"ph-bold ph-check\" aria-hidden=\"true\" style={{ fontSize: 14 }} />}\n      {!selected && icon && <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 16 }} />}\n      {label}\n      {onRemove && (\n        <span\n          role=\"button\" aria-label={`Remove ${label}`} onClick={(e) => { e.stopPropagation(); onRemove(); }}\n          style={{ display: 'inline-flex', marginRight: -2, fontSize: 14, opacity: 0.8 }}\n        ><i className=\"ph ph-x\" aria-hidden=\"true\" /></span>\n      )}\n    </Tag>\n  );\n}\n","components/confirmations/ReservationConfirmationCard.jsx":"import React from 'react';\n\n/**\n * ReservationConfirmationCard — success summary after booking a table:\n * restaurant, date/time, party size, confirmation code.\n */\nexport function ReservationConfirmationCard({\n  restaurantName = '',\n  dateLabel = '',\n  timeLabel = '',\n  partySize = 2,\n  confirmationCode = '',\n}) {\n  const row = (icon, label) => (\n    <div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', color: 'var(--color-text-secondary)', fontSize: 'var(--font-size-body)' }}>\n      <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 18, color: 'var(--color-text-tertiary)' }} />{label}\n    </div>\n  );\n  return (\n    <div style={{\n      padding: 'var(--space-lg)', background: 'var(--card-bg)', border: 'var(--border-width-default) solid var(--card-border)',\n      borderRadius: 'var(--radius-card)', boxShadow: 'var(--elevation-card)', textAlign: 'center', fontFamily: 'var(--font-family-base)',\n    }}>\n      <div style={{\n        width: 56, height: 56, margin: '0 auto var(--space-md)', borderRadius: 'var(--radius-full)',\n        background: 'var(--color-bg-success)', color: 'var(--color-status-success)', display: 'flex',\n        alignItems: 'center', justifyContent: 'center', fontSize: 28,\n      }}><i className=\"ph-fill ph-check-circle\" aria-hidden=\"true\" /></div>\n      <h3 style={{ margin: 0, fontSize: 'var(--font-size-h2)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>Table confirmed</h3>\n      <p style={{ margin: '4px 0 var(--space-md)', color: 'var(--color-text-tertiary)', fontSize: 'var(--font-size-body)' }}>{restaurantName}</p>\n      <div style={{ display: 'flex', flexDirection: 'column', gap: 8, alignItems: 'flex-start', margin: '0 auto', width: 'fit-content' }}>\n        {row('calendar', `${dateLabel} · ${timeLabel}`)}\n        {row('users', `${partySize} guests`)}\n        {confirmationCode && row('ticket', `Code ${confirmationCode}`)}\n      </div>\n    </div>\n  );\n}\n","components/data-display/Card.jsx":"import React from 'react';\n\n/**\n * Card — generic container grouping related content with consistent\n * padding, radius and subtle elevation. Optional press behavior.\n */\nexport function Card({\n  children,\n  padding = 'base',    // 'none' | 'sm' | 'base' | 'lg'\n  interactive = false,\n  onClick = null,\n  elevated = true,\n  style = {},\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  const pads = { none: 0, sm: 'var(--space-md)', base: 'var(--space-base)', lg: 'var(--space-lg)' };\n\n  const base = {\n    background: 'var(--card-bg)',\n    border: 'var(--border-width-default) solid var(--card-border)',\n    borderRadius: 'var(--radius-card)',\n    boxShadow: elevated ? 'var(--elevation-card)' : 'none',\n    padding: pads[padding], boxSizing: 'border-box',\n    fontFamily: 'var(--font-family-base)', color: 'var(--color-text-primary)',\n    transition: 'var(--transition-press)',\n    ...(interactive ? {\n      cursor: 'pointer', WebkitTapHighlightColor: 'transparent',\n      transform: pressed ? 'scale(var(--press-scale))' : 'none',\n    } : {}),\n    ...style,\n  };\n\n  const handlers = interactive ? {\n    onClick, role: 'button', tabIndex: 0,\n    onPointerDown: () => setPressed(true),\n    onPointerUp: () => setPressed(false),\n    onPointerLeave: () => setPressed(false),\n  } : {};\n\n  return <div style={base} {...handlers}>{children}</div>;\n}\n","components/data-display/Divider.jsx":"import React from 'react';\n\n/**\n * Divider — thin line separating content sections or list groups.\n * Horizontal by default; supports an inset and an optional centered label.\n */\nexport function Divider({ inset = 0, label = null, vertical = false }) {\n  if (vertical) {\n    return <span aria-hidden=\"true\" style={{ display: 'inline-block', width: 1, alignSelf: 'stretch', background: 'var(--color-border-muted)' }} />;\n  }\n  if (label) {\n    return (\n      <div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-md)', margin: 'var(--space-sm) 0' }}>\n        <span style={{ flex: 1, height: 1, background: 'var(--color-border-muted)' }} />\n        <span style={{\n          fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-caption)',\n          color: 'var(--color-text-tertiary)', fontWeight: 'var(--font-weight-semibold)',\n        }}>{label}</span>\n        <span style={{ flex: 1, height: 1, background: 'var(--color-border-muted)' }} />\n      </div>\n    );\n  }\n  return <hr aria-hidden=\"true\" style={{ border: 0, height: 1, margin: 0, marginLeft: inset, background: 'var(--color-border-muted)' }} />;\n}\n","components/fab/Fab.jsx":"import React from 'react';\n\n/**\n * Fab — Floating Action Button. Circular, elevated, anchored to a screen\n * edge for a single key persistent action (e.g. view cart, new order).\n * Supports an optional label (extended FAB).\n */\nexport function Fab({\n  icon = 'plus',\n  label = null,        // when set, renders an extended (pill) FAB\n  size = 'md',         // 'md' 56 | 'lg' 64\n  disabled = false,\n  onClick = () => {},\n  ariaLabel = 'Action',\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  const dim = size === 'lg' ? 64 : 56;\n  const extended = Boolean(label);\n\n  const style = {\n    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n    gap: 'var(--space-sm)',\n    height: dim, width: extended ? 'auto' : dim,\n    padding: extended ? '0 var(--space-lg)' : 0,\n    border: 'none',\n    background: pressed ? 'var(--fab-bg-active)' : 'var(--fab-bg)',\n    color: 'var(--fab-icon)',\n    borderRadius: 'var(--radius-full)',\n    boxShadow: 'var(--elevation-raised)',\n    cursor: disabled ? 'not-allowed' : 'pointer',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body-lg)',\n    fontWeight: 'var(--font-weight-semibold)',\n    transition: 'var(--transition-press)', WebkitTapHighlightColor: 'transparent',\n  };\n  const clear = () => setPressed(false);\n\n  return (\n    <button\n      type=\"button\" style={style} disabled={disabled} onClick={onClick}\n      aria-label={extended ? undefined : ariaLabel}\n      onPointerDown={() => !disabled && setPressed(true)}\n      onPointerUp={clear} onPointerLeave={clear}\n    >\n      <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 'var(--icon-lg)' }} />\n      {extended && <span>{label}</span>}\n    </button>\n  );\n}\n","components/feedback/BottomSheet.jsx":"import React from 'react';\n\n/**\n * BottomSheet (Modal) — overlay surface sliding up from the bottom for\n * focused tasks or confirmations. Scrim + drag handle; closes on scrim tap.\n * Absolutely positioned — mount inside a position:relative screen frame.\n */\nexport function BottomSheet({\n  open = false,\n  onClose = () => {},\n  title = null,\n  children = null,\n  footer = null,\n}) {\n  return (\n    <div aria-hidden={!open} style={{ position: 'absolute', inset: 0, pointerEvents: open ? 'auto' : 'none', zIndex: 50 }}>\n      <div onClick={onClose} style={{\n        position: 'absolute', inset: 0, background: 'var(--color-bg-scrim)',\n        opacity: open ? 1 : 0, transition: 'opacity var(--duration-base) var(--ease-standard)',\n      }} />\n      <section role=\"dialog\" aria-modal=\"true\" aria-label={title || 'Sheet'} style={{\n        position: 'absolute', left: 0, right: 0, bottom: 0,\n        background: 'var(--color-bg-surface)',\n        borderTopLeftRadius: 'var(--radius-sheet)', borderTopRightRadius: 'var(--radius-sheet)',\n        boxShadow: 'var(--elevation-sheet)', maxHeight: '88%', display: 'flex', flexDirection: 'column',\n        transform: open ? 'translateY(0)' : 'translateY(100%)',\n        transition: 'transform var(--duration-slow) var(--ease-decelerate)',\n        fontFamily: 'var(--font-family-base)',\n      }}>\n        <div style={{ display: 'flex', justifyContent: 'center', paddingTop: 'var(--space-md)' }}>\n          <span aria-hidden=\"true\" style={{ width: 36, height: 4, borderRadius: 'var(--radius-full)', background: 'var(--color-border-strong)' }} />\n        </div>\n        {title && (\n          <header style={{\n            padding: 'var(--space-md) var(--space-lg)', display: 'flex', alignItems: 'center', justifyContent: 'space-between',\n          }}>\n            <h2 style={{ margin: 0, fontSize: 'var(--font-size-h2)', lineHeight: 'var(--line-height-h2)', fontWeight: 'var(--font-weight-semibold)' }}>{title}</h2>\n            <button type=\"button\" onClick={onClose} aria-label=\"Close\" style={{\n              width: 40, height: 40, border: 'none', background: 'transparent', cursor: 'pointer',\n              color: 'var(--color-text-secondary)', fontSize: 22, borderRadius: 'var(--radius-full)',\n            }}><i className=\"ph ph-x\" aria-hidden=\"true\" /></button>\n          </header>\n        )}\n        <div style={{ padding: '0 var(--space-lg) var(--space-lg)', overflowY: 'auto' }}>{children}</div>\n        {footer && (\n          <footer style={{\n            padding: 'var(--space-base) var(--space-lg)',\n            paddingBottom: 'calc(var(--space-lg) + env(safe-area-inset-bottom, 0px))',\n            borderTop: 'var(--border-width-default) solid var(--color-border-muted)',\n          }}>{footer}</footer>\n        )}\n      </section>\n    </div>\n  );\n}\n","components/feedback/Dialog.jsx":"import React from 'react';\n\n/**\n * Dialog / Alert — centered overlay for critical confirmations, blocking\n * background interaction. Optional destructive tone. Mount inside a\n * position:relative screen frame.\n */\nexport function Dialog({\n  open = false,\n  onClose = () => {},\n  title = '',\n  description = null,\n  icon = null,             // phosphor name\n  tone = 'default',        // 'default' | 'destructive'\n  confirmLabel = 'Confirm',\n  cancelLabel = 'Cancel',\n  onConfirm = () => {},\n  children = null,\n}) {\n  const [pressed, setPressed] = React.useState(null);\n  if (!open) return null;\n  const danger = tone === 'destructive';\n\n  const btn = (primary) => ({\n    flex: 1, minHeight: 48, border: primary ? 'none' : 'var(--border-width-default) solid var(--color-border-default)',\n    borderRadius: 'var(--radius-md)',\n    background: primary\n      ? (pressed === 'c' ? (danger ? 'var(--red-600)' : 'var(--button-primary-bg-active)') : (danger ? 'var(--color-status-error)' : 'var(--button-primary-bg)'))\n      : (pressed === 'x' ? 'var(--color-bg-surface-sunken)' : 'transparent'),\n    color: primary ? 'var(--color-text-on-brand)' : 'var(--color-text-primary)',\n    fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)',\n    cursor: 'pointer', transition: 'var(--transition-press)', WebkitTapHighlightColor: 'transparent',\n  });\n\n  return (\n    <div style={{ position: 'absolute', inset: 0, zIndex: 60, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 'var(--space-lg)' }}>\n      <div onClick={onClose} style={{ position: 'absolute', inset: 0, background: 'var(--color-bg-scrim)' }} />\n      <div role=\"alertdialog\" aria-modal=\"true\" aria-label={title} style={{\n        position: 'relative', width: '100%', maxWidth: 320, background: 'var(--color-bg-surface)',\n        borderRadius: 'var(--radius-dialog)', boxShadow: 'var(--elevation-overlay)',\n        padding: 'var(--space-lg)', boxSizing: 'border-box', textAlign: 'center', fontFamily: 'var(--font-family-base)',\n      }}>\n        {icon && (\n          <div style={{\n            width: 48, height: 48, margin: '0 auto var(--space-md)', display: 'flex', alignItems: 'center', justifyContent: 'center',\n            borderRadius: 'var(--radius-full)', fontSize: 26,\n            background: danger ? 'var(--color-bg-error)' : 'var(--color-brand-subtle)',\n            color: danger ? 'var(--color-status-error)' : 'var(--color-text-accent)',\n          }}><i className={`ph ph-${icon}`} aria-hidden=\"true\" /></div>\n        )}\n        <h2 style={{ margin: 0, fontSize: 'var(--font-size-h2)', lineHeight: 'var(--line-height-h2)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>{title}</h2>\n        {description && <p style={{ margin: 'var(--space-sm) 0 0', fontSize: 'var(--font-size-body-lg)', lineHeight: 'var(--line-height-body-lg)', color: 'var(--color-text-secondary)' }}>{description}</p>}\n        {children}\n        <div style={{ display: 'flex', gap: 'var(--space-md)', marginTop: 'var(--space-lg)' }}>\n          <button type=\"button\" style={btn(false)} onClick={onClose}\n            onPointerDown={() => setPressed('x')} onPointerUp={() => setPressed(null)} onPointerLeave={() => setPressed(null)}>{cancelLabel}</button>\n          <button type=\"button\" style={btn(true)} onClick={onConfirm}\n            onPointerDown={() => setPressed('c')} onPointerUp={() => setPressed(null)} onPointerLeave={() => setPressed(null)}>{confirmLabel}</button>\n        </div>\n      </div>\n    </div>\n  );\n}\n","components/feedback/EmptyState.jsx":"import React from 'react';\n\n/**\n * EmptyState — icon/message shown when a list or section has no content.\n * Optional primary action.\n */\nexport function EmptyState({\n  icon = 'tray',\n  title = 'Nothing here yet',\n  description = null,\n  actionLabel = null,\n  onAction = () => {},\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  return (\n    <div style={{\n      display: 'flex', flexDirection: 'column', alignItems: 'center', textAlign: 'center',\n      padding: 'var(--space-xxl) var(--space-lg)', fontFamily: 'var(--font-family-base)',\n    }}>\n      <div style={{\n        width: 72, height: 72, display: 'flex', alignItems: 'center', justifyContent: 'center',\n        borderRadius: 'var(--radius-full)', background: 'var(--color-bg-surface-sunken)',\n        color: 'var(--color-text-tertiary)', fontSize: 36, marginBottom: 'var(--space-base)',\n      }}><i className={`ph ph-${icon}`} aria-hidden=\"true\" /></div>\n      <h3 style={{ margin: 0, fontSize: 'var(--font-size-h3)', lineHeight: 'var(--line-height-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>{title}</h3>\n      {description && <p style={{ margin: 'var(--space-sm) 0 0', maxWidth: 260, fontSize: 'var(--font-size-body)', lineHeight: 'var(--line-height-body)', color: 'var(--color-text-tertiary)' }}>{description}</p>}\n      {actionLabel && (\n        <button type=\"button\" onClick={onAction}\n          onPointerDown={() => setPressed(true)} onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n          style={{\n            marginTop: 'var(--space-lg)', height: 48, padding: '0 var(--space-lg)', border: 'none', borderRadius: 'var(--radius-md)',\n            background: pressed ? 'var(--button-primary-bg-active)' : 'var(--button-primary-bg)', color: 'var(--button-primary-text)',\n            fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)',\n            cursor: 'pointer', transition: 'var(--transition-press)',\n          }}>{actionLabel}</button>\n      )}\n    </div>\n  );\n}\n","components/feedback/ErrorState.jsx":"import React from 'react';\n\n/**\n * ErrorState — message with a retry action shown when content fails to load.\n * Redundant to color: uses a warning glyph + text, not hue alone.\n */\nexport function ErrorState({\n  title = 'Something went wrong',\n  description = 'We couldn’t load this. Please try again.',\n  retryLabel = 'Try again',\n  onRetry = () => {},\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  return (\n    <div style={{\n      display: 'flex', flexDirection: 'column', alignItems: 'center', textAlign: 'center',\n      padding: 'var(--space-xxl) var(--space-lg)', fontFamily: 'var(--font-family-base)',\n    }}>\n      <div style={{\n        width: 72, height: 72, display: 'flex', alignItems: 'center', justifyContent: 'center',\n        borderRadius: 'var(--radius-full)', background: 'var(--color-bg-error)',\n        color: 'var(--color-status-error)', fontSize: 36, marginBottom: 'var(--space-base)',\n      }}><i className=\"ph ph-warning\" aria-hidden=\"true\" /></div>\n      <h3 style={{ margin: 0, fontSize: 'var(--font-size-h3)', lineHeight: 'var(--line-height-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>{title}</h3>\n      <p style={{ margin: 'var(--space-sm) 0 0', maxWidth: 260, fontSize: 'var(--font-size-body)', lineHeight: 'var(--line-height-body)', color: 'var(--color-text-tertiary)' }}>{description}</p>\n      <button type=\"button\" onClick={onRetry}\n        onPointerDown={() => setPressed(true)} onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n        style={{\n          marginTop: 'var(--space-lg)', display: 'inline-flex', alignItems: 'center', gap: 'var(--space-sm)',\n          height: 48, padding: '0 var(--space-lg)', borderRadius: 'var(--radius-md)',\n          border: 'var(--border-width-default) solid var(--color-border-default)',\n          background: pressed ? 'var(--color-bg-surface-sunken)' : 'transparent', color: 'var(--color-text-primary)',\n          fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)',\n          cursor: 'pointer', transition: 'var(--transition-press)',\n        }}>\n        <i className=\"ph ph-arrow-clockwise\" aria-hidden=\"true\" style={{ fontSize: 18 }} />{retryLabel}\n      </button>\n    </div>\n  );\n}\n","components/feedback/ProgressBar.jsx":"import React from 'react';\n\n/**\n * ProgressBar — horizontal linear progress. Determinate (value 0–100) or\n * indeterminate (animated sweep). Optional label + percentage.\n */\nexport function ProgressBar({ value = 0, indeterminate = false, label = null, showValue = false, tone = 'brand' }) {\n  const colors = { brand: 'var(--color-brand-primary)', success: 'var(--color-status-success)' };\n  const fill = colors[tone] || colors.brand;\n  const pct = Math.max(0, Math.min(100, value));\n\n  return (\n    <div style={{ width: '100%', fontFamily: 'var(--font-family-base)' }}>\n      {(label || showValue) && (\n        <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 'var(--space-sm)', fontSize: 'var(--font-size-caption)', color: 'var(--color-text-secondary)' }}>\n          <span>{label}</span>{showValue && !indeterminate && <span style={{ fontVariantNumeric: 'tabular-nums' }}>{pct}%</span>}\n        </div>\n      )}\n      <div role=\"progressbar\" aria-valuenow={indeterminate ? undefined : pct} aria-valuemin={0} aria-valuemax={100}\n        style={{ position: 'relative', height: 6, borderRadius: 'var(--radius-full)', background: 'var(--color-bg-surface-sunken)', overflow: 'hidden' }}>\n        {indeterminate ? (\n          <span style={{ position: 'absolute', top: 0, bottom: 0, width: '40%', borderRadius: 'var(--radius-full)', background: fill, animation: 'gastro-indet 1.3s var(--ease-standard) infinite' }} />\n        ) : (\n          <span style={{ display: 'block', height: '100%', width: `${pct}%`, background: fill, borderRadius: 'var(--radius-full)', transition: 'width var(--duration-base) var(--ease-standard)' }} />\n        )}\n      </div>\n      <style>{'@keyframes gastro-indet{0%{left:-40%}100%{left:100%}}'}</style>\n    </div>\n  );\n}\n","components/feedback/Skeleton.jsx":"import React from 'react';\n\n/**\n * Skeleton — placeholder shapes mimicking content layout while data loads.\n * Compose primitives (text lines, blocks, circles) with a shimmer.\n */\nexport function Skeleton({ variant = 'text', width = '100%', height = null, radius = null, lines = 3 }) {\n  const base = {\n    background: 'linear-gradient(90deg, var(--gray-100) 25%, var(--gray-200) 37%, var(--gray-100) 63%)',\n    backgroundSize: '400% 100%', animation: 'gastro-shimmer 1.4s ease infinite',\n  };\n  const kf = <style>{'@keyframes gastro-shimmer{0%{background-position:100% 0}100%{background-position:-100% 0}}'}</style>;\n\n  if (variant === 'text') {\n    return (\n      <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-sm)', width }}>\n        {Array.from({ length: lines }).map((_, i) => (\n          <span key={i} style={{ ...base, height: 12, borderRadius: 'var(--radius-xs)', width: i === lines - 1 ? '60%' : '100%' }} />\n        ))}\n        {kf}\n      </div>\n    );\n  }\n  if (variant === 'circle') {\n    const d = height || width;\n    return <span style={{ ...base, display: 'inline-block', width: d, height: d, borderRadius: '50%' }}>{kf}</span>;\n  }\n  // block\n  return <span style={{ ...base, display: 'block', width, height: height || 96, borderRadius: radius || 'var(--radius-md)' }}>{kf}</span>;\n}\n","components/feedback/Spinner.jsx":"import React from 'react';\n\n/**\n * Spinner — circular indeterminate loading indicator. Sizes in px; inherits\n * accent by default, or `onDark` for placement on dark surfaces / buttons.\n */\nexport function Spinner({ size = 24, thickness = 2.5, onDark = false, label = 'Loading' }) {\n  const track = onDark ? 'var(--alpha-white-24)' : 'var(--color-border-default)';\n  const head = onDark ? '#fff' : 'var(--color-brand-primary)';\n  return (\n    <span role=\"status\" aria-label={label} style={{ display: 'inline-flex', width: size, height: size }}>\n      <span style={{\n        width: size, height: size, borderRadius: '50%',\n        border: `${thickness}px solid ${track}`, borderTopColor: head,\n        animation: 'gastro-spin .7s linear infinite',\n      }} />\n      <style>{'@keyframes gastro-spin{to{transform:rotate(360deg)}}'}</style>\n    </span>\n  );\n}\n","components/feedback/Toast.jsx":"import React from 'react';\n\n/**\n * Toast / Snackbar — brief, non-blocking message confirming an action.\n * Anchored bottom; supports a status tone, optional action and auto-dismiss.\n * Render conditionally; `open` drives the enter/exit transition.\n */\nexport function Toast({\n  open = false,\n  message = '',\n  tone = 'default',        // 'default' | 'success' | 'error'\n  actionLabel = null,\n  onAction = () => {},\n  onDismiss = null,\n}) {\n  const icons = { success: 'check-circle', error: 'warning-circle', default: null };\n  const colors = { success: 'var(--color-status-success)', error: 'var(--color-status-error)', default: null };\n  const icon = icons[tone];\n\n  return (\n    <div aria-live=\"polite\" style={{\n      position: 'absolute', left: 'var(--space-base)', right: 'var(--space-base)', bottom: 'var(--space-base)',\n      zIndex: 70, pointerEvents: open ? 'auto' : 'none',\n      transform: open ? 'translateY(0)' : 'translateY(150%)', opacity: open ? 1 : 0,\n      transition: 'transform var(--duration-base) var(--ease-decelerate), opacity var(--duration-base) var(--ease-standard)',\n    }}>\n      <div style={{\n        display: 'flex', alignItems: 'center', gap: 'var(--space-md)',\n        minHeight: 48, padding: '0 var(--space-base)', boxSizing: 'border-box',\n        background: 'var(--toast-bg)', color: 'var(--toast-text)',\n        borderRadius: 'var(--radius-md)', boxShadow: 'var(--elevation-overlay)',\n        fontFamily: 'var(--font-family-base)',\n      }}>\n        {icon && <i className={`ph-fill ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 'var(--icon-sm)', color: colors[tone] }} />}\n        <span style={{ flex: 1, fontSize: 'var(--font-size-body)', lineHeight: 'var(--line-height-body)' }}>{message}</span>\n        {actionLabel && (\n          <button type=\"button\" onClick={onAction} style={{\n            border: 'none', background: 'transparent', color: 'var(--accent-300)', cursor: 'pointer',\n            fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)',\n          }}>{actionLabel}</button>\n        )}\n        {onDismiss && (\n          <button type=\"button\" onClick={onDismiss} aria-label=\"Dismiss\" style={{\n            border: 'none', background: 'transparent', color: 'var(--alpha-white-24)', cursor: 'pointer', fontSize: 18,\n          }}><i className=\"ph ph-x\" aria-hidden=\"true\" /></button>\n        )}\n      </div>\n    </div>\n  );\n}\n","components/filter/Filter.jsx":"import React from 'react';\n\n/**\n * Filter — round icon + label toggle for filter rows on discovery/search\n * screens (cuisine, category, dietary…). Selected state fills with accent.\n */\nexport function Filter({ label = '', icon = 'fork-knife', selected = false, onClick = () => {} }) {\n  return (\n    <button type=\"button\" onClick={onClick} aria-pressed={selected} style={{\n      display: 'inline-flex', flexDirection: 'column', alignItems: 'center', gap: 6,\n      border: 'none', background: 'transparent', cursor: 'pointer', padding: 4, width: 64,\n      WebkitTapHighlightColor: 'transparent', fontFamily: 'var(--font-family-base)',\n    }}>\n      <span style={{\n        width: 48, height: 48, display: 'flex', alignItems: 'center', justifyContent: 'center',\n        borderRadius: 'var(--radius-full)', fontSize: 22,\n        background: selected ? 'var(--color-brand-primary)' : 'var(--color-bg-surface-sunken)',\n        color: selected ? 'var(--color-text-on-brand)' : 'var(--color-text-secondary)',\n        transition: 'var(--transition-colors)',\n      }}><i className={`ph ph-${icon}`} aria-hidden=\"true\" /></span>\n      <span style={{\n        fontSize: 'var(--font-size-caption)', fontWeight: selected ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n        color: selected ? 'var(--color-text-primary)' : 'var(--color-text-tertiary)', textAlign: 'center',\n      }}>{label}</span>\n    </button>\n  );\n}\n","components/filter/FilterBar.jsx":"import React from 'react';\n\n/**\n * FilterBar — horizontal pill toggle: icon on the left, label on the\n * right. Alternative to Filter (round icon-over-label) for denser filter\n * rows. Selected state fills with accent, same as Filter.\n */\nexport function FilterBar({ label = '', icon = 'fork-knife', selected = false, onClick = () => {} }) {\n  return (\n    <button type=\"button\" onClick={onClick} aria-pressed={selected} style={{\n      display: 'inline-flex', alignItems: 'center', gap: 8, height: 40,\n      padding: '0 var(--space-md)', border: 'none', borderRadius: 'var(--radius-full)',\n      background: selected ? 'var(--color-brand-primary)' : 'var(--color-bg-surface-sunken)',\n      color: selected ? 'var(--color-text-on-brand)' : 'var(--color-text-secondary)',\n      cursor: 'pointer', WebkitTapHighlightColor: 'transparent', fontFamily: 'var(--font-family-base)',\n      transition: 'var(--transition-colors)',\n    }}>\n      <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 18 }} />\n      <span style={{\n        fontSize: 'var(--font-size-body)',\n        fontWeight: selected ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n      }}>{label}</span>\n    </button>\n  );\n}\n","components/forms/Checkbox.jsx":"import React from 'react';\n\n/**\n * Checkbox — binary selection control for multi-select lists / agreements.\n * Redundant to color: uses a check glyph, not hue alone.\n */\nexport function Checkbox({\n  checked = false,\n  indeterminate = false,\n  onChange = () => {},\n  label = null,\n  disabled = false,\n  id,\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  const on = checked || indeterminate;\n\n  const box = {\n    flex: '0 0 auto', width: 22, height: 22, display: 'inline-flex',\n    alignItems: 'center', justifyContent: 'center',\n    background: on ? 'var(--control-on-bg)' : 'var(--control-off-bg)',\n    border: `2px solid ${on ? 'var(--control-on-bg)' : 'var(--control-off-border)'}`,\n    borderRadius: 'var(--radius-checkbox)', color: 'var(--control-on-mark)',\n    transition: 'var(--transition-colors)', fontSize: 15,\n    boxShadow: pressed ? '0 0 0 6px var(--control-glow)' : 'none',\n  };\n  const row = {\n    display: 'inline-flex', alignItems: 'center', gap: 'var(--space-md)',\n    cursor: disabled ? 'not-allowed' : 'pointer', minHeight: 'var(--touch-target-min)',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    fontFamily: 'var(--font-family-base)', WebkitTapHighlightColor: 'transparent',\n  };\n  const clear = () => setPressed(false);\n\n  return (\n    <label style={row} htmlFor={id}\n      onPointerDown={() => !disabled && setPressed(true)} onPointerUp={clear} onPointerLeave={clear}>\n      <input id={id} type=\"checkbox\" checked={checked} disabled={disabled}\n        onChange={(e) => onChange(e.target.checked)}\n        style={{ position: 'absolute', opacity: 0, width: 0, height: 0 }}\n        aria-checked={indeterminate ? 'mixed' : checked} />\n      <span style={box} aria-hidden=\"true\">\n        {indeterminate ? <i className=\"ph-bold ph-minus\" /> : checked ? <i className=\"ph-bold ph-check\" /> : null}\n      </span>\n      {label && <span style={{ fontSize: 'var(--font-size-body-lg)', color: 'var(--color-text-primary)' }}>{label}</span>}\n    </label>\n  );\n}\n","components/forms/FormGroup.jsx":"import React from 'react';\n\n/**\n * FormGroup — layout container grouping related fields with a shared\n * label/legend and consistent vertical spacing.\n */\nexport function FormGroup({ legend = null, helperText = null, children }) {\n  return (\n    <fieldset style={{\n      border: 'none', margin: 0, padding: 0, minInlineSize: 'auto',\n      fontFamily: 'var(--font-family-base)',\n    }}>\n      {legend && (\n        <legend style={{\n          padding: 0, marginBottom: 'var(--space-md)',\n          fontSize: 'var(--font-size-h3)', lineHeight: 'var(--line-height-h3)',\n          fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)',\n        }}>{legend}</legend>\n      )}\n      <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-base)' }}>\n        {children}\n      </div>\n      {helperText && (\n        <div style={{\n          marginTop: 'var(--space-sm)', fontSize: 'var(--font-size-caption)',\n          lineHeight: 'var(--line-height-caption)', color: 'var(--color-text-tertiary)',\n        }}>{helperText}</div>\n      )}\n    </fieldset>\n  );\n}\n","components/forms/Radio.jsx":"import React from 'react';\n\n/**\n * Radio — single choice among a small mutually-exclusive set.\n * Use RadioGroup to manage selection across options.\n */\nexport function Radio({\n  checked = false,\n  onChange = () => {},\n  label = null,\n  value,\n  name,\n  disabled = false,\n  id,\n}) {\n  const [pressed, setPressed] = React.useState(false);\n\n  const outer = {\n    boxSizing: 'border-box', flex: '0 0 auto', width: 22, height: 22, borderRadius: '50%',\n    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n    border: `2px solid ${checked ? 'var(--control-on-bg)' : 'var(--control-off-border)'}`,\n    background: 'var(--control-off-bg)', transition: 'var(--transition-colors)',\n    boxShadow: pressed ? '0 0 0 6px var(--control-glow)' : 'none',\n  };\n  const dot = {\n    boxSizing: 'border-box', flex: '0 0 auto', width: 10, height: 10, borderRadius: '50%',\n    background: checked ? 'var(--control-on-bg)' : 'transparent',\n    transition: 'transform var(--duration-fast) var(--ease-emphasized)',\n    transform: checked ? 'scale(1)' : 'scale(0)',\n  };\n  const row = {\n    display: 'inline-flex', alignItems: 'center', gap: 'var(--space-md)',\n    cursor: disabled ? 'not-allowed' : 'pointer', minHeight: 'var(--touch-target-min)',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    fontFamily: 'var(--font-family-base)', WebkitTapHighlightColor: 'transparent',\n  };\n  const clear = () => setPressed(false);\n\n  return (\n    <label style={row} htmlFor={id}\n      onPointerDown={() => !disabled && setPressed(true)} onPointerUp={clear} onPointerLeave={clear}>\n      <input id={id} type=\"radio\" name={name} value={value} checked={checked} disabled={disabled}\n        onChange={() => onChange(value)} style={{ position: 'absolute', opacity: 0, width: 0, height: 0 }} />\n      <span style={outer} aria-hidden=\"true\"><span style={dot} /></span>\n      {label && <span style={{ fontSize: 'var(--font-size-body-lg)', color: 'var(--color-text-primary)' }}>{label}</span>}\n    </label>\n  );\n}\n\n/**\n * RadioGroup — convenience wrapper managing a single selected value\n * across an array of { label, value } options.\n */\nexport function RadioGroup({ options = [], value = null, onChange = () => {}, name = 'radio-group' }) {\n  return (\n    <div role=\"radiogroup\" style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-xs)' }}>\n      {options.map((o) => {\n        const opt = typeof o === 'string' ? { label: o, value: o } : o;\n        return (\n          <Radio key={String(opt.value)} name={name} value={opt.value}\n            checked={value === opt.value} onChange={onChange} label={opt.label} />\n        );\n      })}\n    </div>\n  );\n}\n","components/forms/Slider.jsx":"import React from 'react';\n\n/**\n * Slider — draggable control for selecting a value within a range\n * (e.g. price ceiling, max delivery distance). Single thumb.\n */\nexport function Slider({\n  value = 50,\n  min = 0,\n  max = 100,\n  step = 1,\n  onChange = () => {},\n  disabled = false,\n  formatValue = null,   // (v) => string\n  showValue = true,\n  id,\n}) {\n  const pct = ((value - min) / (max - min)) * 100;\n\n  return (\n    <div style={{ width: '100%', opacity: disabled ? 'var(--state-disabled-opacity)' : 1 }}>\n      {showValue && (\n        <div style={{\n          marginBottom: 'var(--space-sm)', fontFamily: 'var(--font-family-base)',\n          fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)',\n          color: 'var(--color-text-primary)', fontVariantNumeric: 'tabular-nums',\n        }}>{formatValue ? formatValue(value) : value}</div>\n      )}\n      <input\n        id={id} type=\"range\" min={min} max={max} step={step} value={value} disabled={disabled}\n        onChange={(e) => onChange(Number(e.target.value))}\n        style={{\n          width: '100%', height: 24, WebkitAppearance: 'none', appearance: 'none',\n          background: 'transparent', cursor: disabled ? 'not-allowed' : 'pointer',\n          '--pct': `${pct}%`,\n        }}\n      />\n      <style>{`\n        input[type=range]{--track:6px}\n        input[type=range]::-webkit-slider-runnable-track{height:var(--track);border-radius:var(--radius-full);\n          background:linear-gradient(to right,var(--color-brand-primary) var(--pct),var(--color-bg-surface-sunken) var(--pct));}\n        input[type=range]::-moz-range-track{height:var(--track);border-radius:var(--radius-full);background:var(--color-bg-surface-sunken);}\n        input[type=range]::-moz-range-progress{height:var(--track);border-radius:var(--radius-full);background:var(--color-brand-primary);}\n        input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;width:22px;height:22px;margin-top:-8px;\n          border-radius:50%;background:var(--color-bg-main);border:2px solid var(--color-brand-primary);box-shadow:var(--elevation-card);}\n        input[type=range]::-moz-range-thumb{width:22px;height:22px;border-radius:50%;background:var(--color-bg-main);\n          border:2px solid var(--color-brand-primary);box-shadow:var(--elevation-card);}\n      `}</style>\n    </div>\n  );\n}\n","components/forms/Stepper.jsx":"import React from 'react';\n\n/**\n * Stepper — increment/decrement control for quantity selection.\n * Neutral/base level; brand applied via tokens.\n */\nexport function Stepper({\n  value = 0,\n  min = 0,\n  max = 99,\n  step = 1,\n  size = 'md',\n  disabled = false,\n  onChange = () => {},\n  ariaLabel = 'Quantity',\n}) {\n  const [pressed, setPressed] = React.useState(null); // 'dec' | 'inc' | null\n\n  const dims = size === 'sm'\n    ? { h: 32, btn: 32, val: 32, font: 13, icon: 16 }\n    : { h: 40, btn: 40, val: 44, font: 15, icon: 18 };\n\n  const atMin = disabled || value <= min;\n  const atMax = disabled || value >= max;\n\n  const clamp = (v) => Math.max(min, Math.min(max, v));\n  const dec = () => { if (!atMin) onChange(clamp(value - step)); };\n  const inc = () => { if (!atMax) onChange(clamp(value + step)); };\n\n  const wrap = {\n    display: 'inline-flex',\n    alignItems: 'center',\n    height: dims.h,\n    border: `var(--border-width-default) solid var(--color-border-default)`,\n    borderRadius: 'var(--radius-full)',\n    background: 'var(--color-bg-main)',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    userSelect: 'none',\n  };\n\n  const btn = (isActive, isPressed) => ({\n    width: dims.btn,\n    height: dims.btn,\n    display: 'inline-flex',\n    alignItems: 'center',\n    justifyContent: 'center',\n    border: 'none',\n    background: isPressed ? 'var(--overlay-press-dark)' : 'transparent',\n    borderRadius: 'var(--radius-full)',\n    color: isActive ? 'var(--color-text-primary)' : 'var(--color-text-disabled)',\n    cursor: isActive ? 'pointer' : 'not-allowed',\n    padding: 0,\n    fontSize: dims.icon,\n    transition: 'var(--transition-press)',\n    WebkitTapHighlightColor: 'transparent',\n  });\n\n  const val = {\n    minWidth: dims.val,\n    textAlign: 'center',\n    fontFamily: 'var(--font-family-base)',\n    fontSize: dims.font,\n    fontWeight: 'var(--font-weight-semibold)',\n    color: 'var(--color-text-primary)',\n    fontVariantNumeric: 'tabular-nums',\n  };\n\n  return (\n    <div style={wrap} role=\"group\" aria-label={ariaLabel}>\n      <button\n        type=\"button\"\n        style={btn(!atMin, pressed === 'dec')}\n        onClick={dec}\n        onPointerDown={() => !atMin && setPressed('dec')}\n        onPointerUp={() => setPressed(null)}\n        onPointerLeave={() => setPressed(null)}\n        disabled={atMin}\n        aria-label=\"Decrease\"\n      >\n        <i className=\"ph ph-minus\" aria-hidden=\"true\" />\n      </button>\n      <span style={val} aria-live=\"polite\">{value}</span>\n      <button\n        type=\"button\"\n        style={btn(!atMax, pressed === 'inc')}\n        onClick={inc}\n        onPointerDown={() => !atMax && setPressed('inc')}\n        onPointerUp={() => setPressed(null)}\n        onPointerLeave={() => setPressed(null)}\n        disabled={atMax}\n        aria-label=\"Increase\"\n      >\n        <i className=\"ph ph-plus\" aria-hidden=\"true\" />\n      </button>\n    </div>\n  );\n}\n","components/forms/Switch.jsx":"import React from 'react';\n\n/**\n * Switch — on/off toggle for settings and binary preferences.\n * Immediate (<100ms) thumb slide. Redundant to color via position.\n */\nexport function Switch({\n  checked = false,\n  onChange = () => {},\n  label = null,\n  disabled = false,\n  id,\n}) {\n  const track = {\n    position: 'relative', flex: '0 0 auto', width: 44, height: 26,\n    borderRadius: 'var(--radius-full)',\n    background: checked ? 'var(--control-on-bg)' : 'var(--color-border-strong)',\n    transition: 'background-color var(--duration-fast) var(--ease-standard)',\n  };\n  const thumb = {\n    position: 'absolute', top: 3, left: 3, width: 20, height: 20, borderRadius: '50%',\n    background: 'var(--color-bg-main)', boxShadow: 'var(--elevation-card)',\n    transform: checked ? 'translateX(18px)' : 'translateX(0)',\n    transition: 'transform var(--duration-fast) var(--ease-standard)',\n  };\n  const row = {\n    display: 'inline-flex', alignItems: 'center', gap: 'var(--space-md)',\n    cursor: disabled ? 'not-allowed' : 'pointer', minHeight: 'var(--touch-target-min)',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    fontFamily: 'var(--font-family-base)', WebkitTapHighlightColor: 'transparent',\n  };\n\n  return (\n    <label style={row} htmlFor={id}>\n      <input id={id} type=\"checkbox\" role=\"switch\" checked={checked} disabled={disabled}\n        onChange={(e) => onChange(e.target.checked)}\n        style={{ position: 'absolute', opacity: 0, width: 0, height: 0 }} />\n      <span style={track} aria-hidden=\"true\"><span style={thumb} /></span>\n      {label && <span style={{ fontSize: 'var(--font-size-body-lg)', color: 'var(--color-text-primary)' }}>{label}</span>}\n    </label>\n  );\n}\n","components/icon-button/IconButton.jsx":"import React from 'react';\n\n/**\n * IconButton — tappable icon-only control (favorite, share, close, back).\n * Always renders inside a >=48px hit target even when the visual is smaller.\n */\nexport function IconButton({\n  icon = 'heart',      // phosphor name\n  variant = 'ghost',   // 'ghost' | 'filled' | 'tonal'\n  size = 'md',         // 'sm' 40 | 'md' 48 | 'lg' 56 hit target\n  disabled = false,\n  active = false,      // e.g. favorited\n  onClick = () => {},\n  ariaLabel = 'Action',\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  const targets = { sm: 40, md: 48, lg: 56 };\n  const icons = { sm: 20, md: 24, lg: 24 };\n  const t = targets[size];\n\n  const palettes = {\n    ghost: {\n      bg: pressed ? 'var(--overlay-press-dark)' : 'transparent',\n      color: active ? 'var(--color-brand-primary)' : 'var(--color-text-primary)',\n    },\n    filled: {\n      bg: pressed ? 'var(--button-primary-bg-active)' : 'var(--button-primary-bg)',\n      color: 'var(--button-primary-text)',\n    },\n    tonal: {\n      bg: pressed ? 'var(--accent-100)' : 'var(--color-brand-subtle)',\n      color: 'var(--color-text-accent)',\n    },\n  };\n  const p = palettes[variant] || palettes.ghost;\n\n  const style = {\n    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n    width: t, height: t, padding: 0, border: 'none',\n    background: p.bg, color: p.color, borderRadius: 'var(--radius-full)',\n    cursor: disabled ? 'not-allowed' : 'pointer',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    transition: 'var(--transition-press)', WebkitTapHighlightColor: 'transparent',\n  };\n  const clear = () => setPressed(false);\n\n  return (\n    <button\n      type=\"button\" style={style} disabled={disabled} onClick={onClick}\n      aria-label={ariaLabel} aria-pressed={active || undefined}\n      onPointerDown={() => !disabled && setPressed(true)}\n      onPointerUp={clear} onPointerLeave={clear}\n    >\n      <i\n        className={`ph${active && variant === 'ghost' ? '-fill' : ''} ph-${icon}`}\n        aria-hidden=\"true\"\n        style={{ fontSize: icons[size] }}\n      />\n    </button>\n  );\n}\n","components/inputs/Accordion.jsx":"import React from 'react';\n\n/**\n * Accordion — collapsible section revealing content on tap. Manages a list\n * of items; single-open by default, or `allowMultiple`.\n */\nexport function Accordion({ items = [], allowMultiple = false, defaultOpen = [] }) {\n  const [open, setOpen] = React.useState(new Set(defaultOpen));\n\n  const toggle = (i) => {\n    setOpen((prev) => {\n      const next = new Set(allowMultiple ? prev : []);\n      if (prev.has(i)) next.delete(i); else next.add(i);\n      return next;\n    });\n  };\n\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      {items.map((it, i) => {\n        const isOpen = open.has(i);\n        return (\n          <div key={i} style={{ borderBottom: 'var(--border-width-default) solid var(--color-border-muted)' }}>\n            <button type=\"button\" onClick={() => toggle(i)} aria-expanded={isOpen} style={{\n              display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 'var(--space-md)',\n              width: '100%', minHeight: 56, padding: 'var(--space-md) 0', border: 'none', background: 'transparent',\n              cursor: 'pointer', textAlign: 'left', color: 'var(--color-text-primary)',\n              fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)', WebkitTapHighlightColor: 'transparent',\n            }}>\n              <span>{it.title}</span>\n              <i className=\"ph ph-caret-down\" aria-hidden=\"true\" style={{\n                fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)', flex: '0 0 auto',\n                transform: isOpen ? 'rotate(180deg)' : 'none', transition: 'transform var(--duration-base) var(--ease-standard)',\n              }} />\n            </button>\n            <div style={{\n              overflow: 'hidden', maxHeight: isOpen ? 400 : 0,\n              transition: 'max-height var(--duration-slow) var(--ease-standard)',\n            }}>\n              <div style={{ paddingBottom: 'var(--space-base)', fontSize: 'var(--font-size-body)', lineHeight: 'var(--line-height-body)', color: 'var(--color-text-secondary)' }}>\n                {it.content}\n              </div>\n            </div>\n          </div>\n        );\n      })}\n    </div>\n  );\n}\n","components/inputs/Dropdown.jsx":"import React from 'react';\n\n/**\n * Dropdown / Select — tappable field that opens a list of options.\n * Neutral/base level; brand applied via tokens.\n *\n * options: array of { label, value } or plain strings.\n */\nexport function Dropdown({\n  options = [],\n  value = null,\n  placeholder = 'Select…',\n  label = null,\n  helperText = null,\n  disabled = false,\n  error = false,\n  size = 'fixed', // 'fixed' (48px) | 'floating' (56px)\n  onChange = () => {},\n  id,\n}) {\n  const [open, setOpen] = React.useState(false);\n  const rootRef = React.useRef(null);\n\n  const norm = options.map((o) =>\n    typeof o === 'string' ? { label: o, value: o } : o\n  );\n  const selected = norm.find((o) => o.value === value) || null;\n  const height = size === 'floating' ? 56 : 48;\n\n  React.useEffect(() => {\n    if (!open) return;\n    const onDoc = (e) => {\n      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);\n    };\n    document.addEventListener('pointerdown', onDoc);\n    return () => document.removeEventListener('pointerdown', onDoc);\n  }, [open]);\n\n  const borderColor = error\n    ? 'var(--input-border-error)'\n    : open\n    ? 'var(--input-border-focus)'\n    : 'var(--input-border)';\n\n  const field = {\n    display: 'flex',\n    alignItems: 'center',\n    gap: 'var(--space-sm)',\n    width: '100%',\n    height,\n    padding: '0 var(--input-padding-x)',\n    boxSizing: 'border-box',\n    background: 'var(--input-bg)',\n    border: `${open || error ? 'var(--border-width-focus)' : 'var(--border-width-default)'} solid ${borderColor}`,\n    borderRadius: 'var(--radius-input)',\n    color: selected ? 'var(--input-text)' : 'var(--input-placeholder)',\n    fontFamily: 'var(--font-family-base)',\n    fontSize: 'var(--font-size-body)',\n    fontWeight: 'var(--font-weight-medium)',\n    cursor: disabled ? 'not-allowed' : 'pointer',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    textAlign: 'left',\n    WebkitTapHighlightColor: 'transparent',\n  };\n\n  const menu = {\n    position: 'absolute',\n    top: `calc(100% + var(--space-xs))`,\n    left: 0,\n    right: 0,\n    zIndex: 20,\n    background: 'var(--color-bg-surface)',\n    border: 'var(--border-width-default) solid var(--color-border-muted)',\n    borderRadius: 'var(--radius-input)',\n    boxShadow: 'var(--elevation-overlay)',\n    padding: 'var(--space-xs)',\n    maxHeight: 240,\n    overflowY: 'auto',\n  };\n\n  const opt = (isSel) => ({\n    display: 'flex',\n    alignItems: 'center',\n    justifyContent: 'space-between',\n    width: '100%',\n    minHeight: 44,\n    padding: '0 var(--space-md)',\n    border: 'none',\n    background: isSel ? 'var(--color-brand-subtle)' : 'transparent',\n    borderRadius: 'var(--radius-sm)',\n    color: isSel ? 'var(--color-text-accent)' : 'var(--color-text-primary)',\n    fontFamily: 'var(--font-family-base)',\n    fontSize: 'var(--font-size-body)',\n    fontWeight: isSel ? 'var(--font-weight-semibold)' : 'var(--font-weight-regular)',\n    cursor: 'pointer',\n    textAlign: 'left',\n  });\n\n  return (\n    <div ref={rootRef} style={{ position: 'relative', width: '100%' }}>\n      {label && (\n        <label\n          htmlFor={id}\n          style={{\n            display: 'block',\n            marginBottom: 'var(--space-sm)',\n            fontFamily: 'var(--font-family-base)',\n            fontSize: 'var(--font-size-body)',\n            fontWeight: 'var(--font-weight-medium)',\n            color: 'var(--input-label)',\n          }}\n        >\n          {label}\n        </label>\n      )}\n      <button\n        id={id}\n        type=\"button\"\n        style={field}\n        disabled={disabled}\n        onClick={() => !disabled && setOpen((o) => !o)}\n        aria-haspopup=\"listbox\"\n        aria-expanded={open}\n      >\n        <span style={{ flex: 1, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>\n          {selected ? selected.label : placeholder}\n        </span>\n        <i\n          className=\"ph ph-caret-down\"\n          aria-hidden=\"true\"\n          style={{\n            fontSize: 'var(--icon-sm)',\n            color: 'var(--color-text-tertiary)',\n            transform: open ? 'rotate(180deg)' : 'none',\n            transition: `transform var(--duration-fast) var(--ease-standard)`,\n          }}\n        />\n      </button>\n\n      {open && (\n        <div style={menu} role=\"listbox\">\n          {norm.map((o) => {\n            const isSel = o.value === value;\n            return (\n              <button\n                key={String(o.value)}\n                type=\"button\"\n                role=\"option\"\n                aria-selected={isSel}\n                style={opt(isSel)}\n                onClick={() => { onChange(o.value); setOpen(false); }}\n              >\n                <span>{o.label}</span>\n                {isSel && <i className=\"ph ph-check\" aria-hidden=\"true\" style={{ fontSize: 'var(--icon-sm)' }} />}\n              </button>\n            );\n          })}\n        </div>\n      )}\n\n      {helperText && (\n        <div\n          style={{\n            marginTop: 'var(--space-sm)',\n            display: 'flex',\n            alignItems: 'center',\n            gap: 'var(--space-xs)',\n            fontFamily: 'var(--font-family-base)',\n            fontSize: 'var(--font-size-caption)',\n            lineHeight: 'var(--line-height-caption)',\n            color: error ? 'var(--color-text-error)' : 'var(--input-helper)',\n          }}\n        >\n          {error && <i className=\"ph ph-warning-circle\" aria-hidden=\"true\" style={{ fontSize: 14 }} />}\n          {helperText}\n        </div>\n      )}\n    </div>\n  );\n}\n","components/inputs/TextArea.jsx":"import React from 'react';\n\n/**\n * TextArea — multi-line input for longer free text (special instructions,\n * reviews). Auto-min height, optional character counter.\n */\nexport function TextArea({\n  value = '',\n  onChange = () => {},\n  label = null,\n  placeholder = '',\n  helperText = null,\n  error = false,\n  disabled = false,\n  rows = 4,\n  maxLength = null,\n  id,\n}) {\n  const [focused, setFocused] = React.useState(false);\n  const borderColor = error\n    ? 'var(--input-border-error)'\n    : focused ? 'var(--input-border-focus)' : 'var(--input-border)';\n  const borderW = focused || error ? 'var(--border-width-focus)' : 'var(--border-width-default)';\n\n  const area = {\n    width: '100%', boxSizing: 'border-box', resize: 'vertical',\n    padding: 'var(--space-md) var(--input-padding-x)',\n    background: 'var(--input-bg)', color: 'var(--input-text)',\n    border: `${borderW} solid ${borderColor}`, borderRadius: 'var(--radius-input)',\n    fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)',\n    lineHeight: 'var(--line-height-body)', outline: 'none',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    transition: 'var(--transition-colors)',\n  };\n\n  return (\n    <div style={{ width: '100%' }}>\n      {label && (\n        <label htmlFor={id} style={{\n          display: 'block', marginBottom: 'var(--space-sm)',\n          fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)',\n          fontWeight: 'var(--font-weight-medium)', color: 'var(--input-label)',\n        }}>{label}</label>\n      )}\n      <textarea\n        id={id} rows={rows} value={value} placeholder={placeholder}\n        disabled={disabled} maxLength={maxLength || undefined} style={area}\n        onChange={(e) => onChange(e.target.value)}\n        onFocus={() => setFocused(true)} onBlur={() => setFocused(false)}\n        aria-invalid={error || undefined}\n      />\n      <div style={{\n        marginTop: 'var(--space-sm)', display: 'flex', justifyContent: 'space-between',\n        gap: 'var(--space-sm)', fontFamily: 'var(--font-family-base)',\n        fontSize: 'var(--font-size-caption)', lineHeight: 'var(--line-height-caption)',\n        color: error ? 'var(--color-text-error)' : 'var(--input-helper)',\n      }}>\n        <span style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-xs)' }}>\n          {error && <i className=\"ph ph-warning-circle\" aria-hidden=\"true\" style={{ fontSize: 14 }} />}\n          {helperText}\n        </span>\n        {maxLength && <span style={{ fontVariantNumeric: 'tabular-nums' }}>{value.length}/{maxLength}</span>}\n      </div>\n    </div>\n  );\n}\n","components/inputs/TextInput.jsx":"import React from 'react';\n\n/**\n * TextInput — single-line field with label, placeholder, helper/error text,\n * optional leading/trailing icon. Fixed-label style (48px) by default;\n * `floating` grows to 56px. Neutral/base; brand via tokens.\n */\nexport function TextInput({\n  value = '',\n  onChange = () => {},\n  label = null,\n  placeholder = '',\n  helperText = null,\n  error = false,\n  disabled = false,\n  size = 'fixed',      // 'fixed' 48 | 'floating' 56\n  type = 'text',\n  iconLeft = null,\n  iconRight = null,\n  id,\n}) {\n  const [focused, setFocused] = React.useState(false);\n  const height = size === 'floating' ? 56 : 48;\n\n  const borderColor = error\n    ? 'var(--input-border-error)'\n    : focused ? 'var(--input-border-focus)' : 'var(--input-border)';\n  const borderW = focused || error ? 'var(--border-width-focus)' : 'var(--border-width-default)';\n\n  const wrap = {\n    display: 'flex', alignItems: 'center', gap: 'var(--space-sm)',\n    height, padding: '0 var(--input-padding-x)', boxSizing: 'border-box',\n    background: 'var(--input-bg)',\n    border: `${borderW} solid ${borderColor}`,\n    borderRadius: 'var(--radius-input)',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    transition: 'var(--transition-colors)',\n  };\n  const input = {\n    flex: 1, minWidth: 0, height: '100%', border: 'none', outline: 'none',\n    background: 'transparent', color: 'var(--input-text)',\n    fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)',\n    fontWeight: 'var(--font-weight-regular)',\n  };\n\n  return (\n    <div style={{ width: '100%' }}>\n      {label && (\n        <label htmlFor={id} style={{\n          display: 'block', marginBottom: 'var(--space-sm)',\n          fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)',\n          fontWeight: 'var(--font-weight-medium)', color: 'var(--input-label)',\n        }}>{label}</label>\n      )}\n      <div style={wrap}>\n        {iconLeft && <i className={`ph ph-${iconLeft}`} aria-hidden=\"true\" style={{ fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)' }} />}\n        <input\n          id={id} type={type} value={value} placeholder={placeholder}\n          disabled={disabled} style={input}\n          onChange={(e) => onChange(e.target.value)}\n          onFocus={() => setFocused(true)} onBlur={() => setFocused(false)}\n          aria-invalid={error || undefined}\n        />\n        {iconRight && <i className={`ph ph-${iconRight}`} aria-hidden=\"true\" style={{ fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)' }} />}\n      </div>\n      {helperText && (\n        <div style={{\n          marginTop: 'var(--space-sm)', display: 'flex', alignItems: 'center', gap: 'var(--space-xs)',\n          fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-caption)',\n          lineHeight: 'var(--line-height-caption)',\n          color: error ? 'var(--color-text-error)' : 'var(--input-helper)',\n        }}>\n          {error && <i className=\"ph ph-warning-circle\" aria-hidden=\"true\" style={{ fontSize: 14 }} />}\n          {helperText}\n        </div>\n      )}\n    </div>\n  );\n}\n","components/list-items/AdvancedListItem.jsx":"import React from 'react';\n\n/**\n * AdvancedListItem — single row: optional leading icon/avatar, title,\n * subtitle, and trailing element (value, chevron, control). Row heights\n * follow the spec (48 / 56 / 72 / 88). For a bare label + arrow, use\n * SimpleListItem instead.\n */\nexport function AdvancedListItem({\n  title = '',\n  subtitle = null,\n  leadingIcon = null,      // phosphor name\n  leadingAvatar = null,    // node (e.g. <Avatar/>)\n  trailing = null,         // node\n  chevron = false,\n  onClick = null,\n  disabled = false,\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  const interactive = Boolean(onClick);\n  const twoLine = Boolean(subtitle);\n\n  const row = {\n    display: 'flex', alignItems: 'center', gap: 'var(--space-md)', width: '100%',\n    minHeight: twoLine ? 72 : 56, padding: 'var(--space-md) var(--space-base)', boxSizing: 'border-box',\n    background: pressed ? 'var(--overlay-hover-dark)' : 'transparent',\n    border: 'none', textAlign: 'left', fontFamily: 'var(--font-family-base)',\n    cursor: interactive && !disabled ? 'pointer' : 'default',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    WebkitTapHighlightColor: 'transparent', transition: 'background-color var(--duration-fast) var(--ease-standard)',\n  };\n  const clear = () => setPressed(false);\n  const Tag = interactive ? 'button' : 'div';\n\n  return (\n    <Tag\n      type={interactive ? 'button' : undefined} style={row} onClick={disabled ? undefined : onClick}\n      disabled={interactive ? disabled : undefined}\n      onPointerDown={() => interactive && !disabled && setPressed(true)} onPointerUp={clear} onPointerLeave={clear}\n    >\n      {leadingAvatar}\n      {!leadingAvatar && leadingIcon && (\n        <span style={{\n          flex: '0 0 auto', width: 40, height: 40, display: 'inline-flex', alignItems: 'center',\n          justifyContent: 'center', borderRadius: 'var(--radius-md)', background: 'var(--color-bg-surface-sunken)',\n          color: 'var(--color-text-secondary)', fontSize: 'var(--icon-sm)',\n        }}><i className={`ph ph-${leadingIcon}`} aria-hidden=\"true\" /></span>\n      )}\n      <span style={{ flex: 1, minWidth: 0 }}>\n        <span style={{\n          display: 'block', fontSize: 'var(--font-size-body-lg)', lineHeight: 'var(--line-height-body-lg)',\n          fontWeight: 'var(--font-weight-medium)', color: 'var(--color-text-primary)',\n          overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',\n        }}>{title}</span>\n        {subtitle && (\n          <span style={{\n            display: 'block', fontSize: 'var(--font-size-body)', lineHeight: 'var(--line-height-body)',\n            color: 'var(--color-text-tertiary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',\n          }}>{subtitle}</span>\n        )}\n      </span>\n      {trailing && <span style={{ flex: '0 0 auto', color: 'var(--color-text-secondary)' }}>{trailing}</span>}\n      {chevron && <i className=\"ph ph-caret-right\" aria-hidden=\"true\" style={{ flex: '0 0 auto', fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)' }} />}\n    </Tag>\n  );\n}\n","components/list-items/SimpleListItem.jsx":"import React from 'react';\n\n/**\n * SimpleListItem — minimal navigational row: a text label and a\n * right-pointing chevron. For rows needing icons, subtitles or trailing\n * values, use AdvancedListItem.\n */\nexport function SimpleListItem({ label = '', onClick = null, disabled = false }) {\n  const [pressed, setPressed] = React.useState(false);\n  const interactive = Boolean(onClick);\n  const clear = () => setPressed(false);\n  const Tag = interactive ? 'button' : 'div';\n\n  return (\n    <Tag\n      type={interactive ? 'button' : undefined}\n      onClick={disabled ? undefined : onClick}\n      disabled={interactive ? disabled : undefined}\n      onPointerDown={() => interactive && !disabled && setPressed(true)}\n      onPointerUp={clear} onPointerLeave={clear}\n      style={{\n        display: 'flex', alignItems: 'center', gap: 'var(--space-md)', width: '100%',\n        minHeight: 56, padding: 'var(--space-md) var(--space-base)', boxSizing: 'border-box',\n        background: pressed ? 'var(--overlay-hover-dark)' : 'transparent', border: 'none', textAlign: 'left',\n        fontFamily: 'var(--font-family-base)', cursor: interactive && !disabled ? 'pointer' : 'default',\n        opacity: disabled ? 'var(--state-disabled-opacity)' : 1, WebkitTapHighlightColor: 'transparent',\n        transition: 'background-color var(--duration-fast) var(--ease-standard)',\n      }}\n    >\n      <span style={{\n        flex: 1, minWidth: 0, fontSize: 'var(--font-size-body-lg)', lineHeight: 'var(--line-height-body-lg)',\n        fontWeight: 'var(--font-weight-medium)', color: 'var(--color-text-primary)',\n        overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',\n      }}>{label}</span>\n      <i className=\"ph ph-caret-right\" aria-hidden=\"true\" style={{ flex: '0 0 auto', fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)' }} />\n    </Tag>\n  );\n}\n","components/loyalty/LoyaltyRewardsBadge.jsx":"import React from 'react';\n\n/**\n * LoyaltyRewardsBadge — compact points/tier badge shown in the app bar or\n * profile card (e.g. \"1,240 pts · Gold\"). Optional progress ring to next tier.\n */\nexport function LoyaltyRewardsBadge({ points = 0, tier = null, progress = null, onClick = null }) {\n  const Tag = onClick ? 'button' : 'div';\n  return (\n    <Tag onClick={onClick} type={onClick ? 'button' : undefined} style={{\n      display: 'inline-flex', alignItems: 'center', gap: 'var(--space-sm)', padding: '6px 12px',\n      borderRadius: 'var(--radius-full)', background: 'var(--color-brand-subtle)', color: 'var(--color-text-accent)',\n      border: 'none', cursor: onClick ? 'pointer' : 'default', fontFamily: 'var(--font-family-base)',\n      WebkitTapHighlightColor: 'transparent',\n    }}>\n      <span style={{ position: 'relative', width: 22, height: 22, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>\n        {progress != null && (\n          <svg width=\"22\" height=\"22\" style={{ position: 'absolute', transform: 'rotate(-90deg)' }}>\n            <circle cx=\"11\" cy=\"11\" r=\"9\" fill=\"none\" stroke=\"var(--accent-200)\" strokeWidth=\"2\" />\n            <circle cx=\"11\" cy=\"11\" r=\"9\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\"\n              strokeDasharray={2 * Math.PI * 9} strokeDashoffset={2 * Math.PI * 9 * (1 - progress)} strokeLinecap=\"round\" />\n          </svg>\n        )}\n        <i className=\"ph-fill ph-crown\" aria-hidden=\"true\" style={{ fontSize: 12 }} />\n      </span>\n      <span style={{ fontSize: 'var(--font-size-caption)', fontWeight: 'var(--font-weight-bold)' }}>\n        {points.toLocaleString()} pts{tier ? ` · ${tier}` : ''}\n      </span>\n    </Tag>\n  );\n}\n","components/loyalty/LoyaltyWidget.jsx":"import React from 'react';\n\n/**\n * LoyaltyWidget — membership card: points balance, membership tier\n * (Regular/Advanced/Prime, tier names are brand-configurable), and a QR\n * code for in-store scanning. Own layout: stacked header with tier pill,\n * large points readout, divider, then a centered QR block below.\n */\nexport function LoyaltyWidget({\n  points = 0,\n  membership = 'Regular',\n  discountLabel = null,   // e.g. \"10%\"\n  qrValue = '',\n}) {\n  const size = 21;\n  let seed = 0;\n  for (let i = 0; i < qrValue.length; i++) seed = (seed * 31 + qrValue.charCodeAt(i)) >>> 0;\n  const rand = () => { seed = (seed * 1664525 + 1013904223) >>> 0; return seed / 4294967296; };\n  const inFinder = (r, c) => (r < 7 && c < 7) || (r < 7 && c >= size - 7) || (r >= size - 7 && c < 7);\n  const modules = [];\n  for (let r = 0; r < size; r++) {\n    for (let c = 0; c < size; c++) {\n      if (inFinder(r, c)) continue;\n      if (rand() > 0.55) modules.push([r, c]);\n    }\n  }\n  const finder = (top, left) => (\n    <React.Fragment key={`${top}-${left}`}>\n      <rect x={left} y={top} width={7} height={7} fill=\"#000\" />\n      <rect x={left + 1} y={top + 1} width={5} height={5} fill=\"#fff\" />\n      <rect x={left + 2} y={top + 2} width={3} height={3} fill=\"#000\" />\n    </React.Fragment>\n  );\n\n  return (\n    <div style={{\n      display: 'flex', alignItems: 'center', gap: 'var(--space-lg)',\n      padding: 'var(--space-lg)', borderRadius: 'var(--radius-card)',\n      background: 'var(--card-bg)', border: 'var(--border-width-default) solid var(--card-border)',\n      boxShadow: 'var(--elevation-card)', fontFamily: 'var(--font-family-base)', boxSizing: 'border-box',\n    }}>\n      <div style={{ flex: 1, minWidth: 0, display: 'flex', flexDirection: 'column', gap: 'var(--space-sm)' }}>\n        <span style={{\n          fontSize: 'var(--font-size-overline)', letterSpacing: 'var(--letter-spacing-overline)', textTransform: 'uppercase',\n          fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-tertiary)',\n        }}>Loyalty points</span>\n\n        <div style={{ display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: 6 }}>\n          <span style={{ fontSize: 36, lineHeight: 1, fontWeight: 'var(--font-weight-bold)', color: 'var(--color-text-primary)', fontVariantNumeric: 'tabular-nums' }}>\n            {points.toLocaleString()}\n          </span>\n        </div>\n\n        <span style={{\n          display: 'inline-flex', alignItems: 'center', gap: 6, padding: '4px 12px', alignSelf: 'flex-start',\n          borderRadius: 'var(--radius-full)', background: 'var(--color-brand-subtle)', color: 'var(--color-text-accent)',\n          fontSize: 'var(--font-size-caption)', fontWeight: 'var(--font-weight-bold)',\n        }}>\n          <i className=\"ph-fill ph-crown\" aria-hidden=\"true\" style={{ fontSize: 13 }} />{membership}\n          {discountLabel && <span style={{ color: 'var(--accent-300)' }}>· {discountLabel} off</span>}\n        </span>\n      </div>\n\n      <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 6, flex: '0 0 auto' }}>\n        <div style={{ background: '#fff', padding: 8, borderRadius: 'var(--radius-sm)', border: 'var(--border-width-default) solid var(--color-border-muted)' }}>\n          <svg viewBox={`0 0 ${size} ${size}`} width={88} height={88} shapeRendering=\"crispEdges\">\n            <rect x={0} y={0} width={size} height={size} fill=\"#fff\" />\n            {finder(0, 0)}\n            {finder(0, size - 7)}\n            {finder(size - 7, 0)}\n            {modules.map(([r, c]) => <rect key={`${r}-${c}`} x={c} y={r} width={1} height={1} fill=\"#000\" />)}\n          </svg>\n        </div>\n      </div>\n    </div>\n  );\n}\n","components/media/MapView.jsx":"import React from 'react';\n\n/**\n * MapView — static/placeholder map surface for delivery tracking and\n * address selection. Neutral map-styled background with a pin marker;\n * intended to be swapped for a real map SDK (Mapbox/Google Maps) later.\n */\nexport function MapView({ height = 200, markerLabel = null, showCourier = false }) {\n  return (\n    <div style={{\n      position: 'relative', width: '100%', height, borderRadius: 'var(--radius-card)',\n      overflow: 'hidden', background: `\n        repeating-linear-gradient(0deg, var(--gray-100) 0 1px, transparent 1px 24px),\n        repeating-linear-gradient(90deg, var(--gray-100) 0 1px, transparent 1px 24px),\n        var(--gray-50)`,\n      fontFamily: 'var(--font-family-base)',\n    }}>\n      <div style={{\n        position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -100%)',\n        display: 'flex', flexDirection: 'column', alignItems: 'center',\n      }}>\n        <span style={{\n          width: 36, height: 36, display: 'flex', alignItems: 'center', justifyContent: 'center',\n          borderRadius: '50% 50% 50% 0', transform: 'rotate(-45deg)', background: 'var(--color-brand-primary)',\n          boxShadow: 'var(--elevation-raised)',\n        }}>\n          <i className=\"ph-fill ph-map-pin\" aria-hidden=\"true\" style={{ color: 'var(--color-text-on-brand)', fontSize: 18, transform: 'rotate(45deg)' }} />\n        </span>\n      </div>\n      {showCourier && (\n        <div style={{\n          position: 'absolute', top: '38%', left: '62%', width: 30, height: 30, borderRadius: '50%',\n          background: 'var(--color-bg-main)', boxShadow: 'var(--elevation-card)',\n          display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--color-brand-primary)',\n        }}><i className=\"ph-fill ph-motorcycle\" aria-hidden=\"true\" style={{ fontSize: 16 }} /></div>\n      )}\n      {markerLabel && (\n        <div style={{\n          position: 'absolute', bottom: 12, left: 12, right: 12, background: 'var(--color-bg-main)',\n          borderRadius: 'var(--radius-md)', padding: '8px 12px', boxShadow: 'var(--elevation-card)',\n          fontSize: 'var(--font-size-caption)', color: 'var(--color-text-secondary)',\n        }}>{markerLabel}</div>\n      )}\n    </div>\n  );\n}\n","components/menu/CartLineItem.jsx":"import React from 'react';\nimport { Stepper } from '../forms/Stepper.jsx';\n\n/**\n * CartLineItem — single row in the cart/order list: thumbnail, name,\n * modifiers summary, price, and a quantity Stepper (or read-only qty for\n * order history).\n */\nexport function CartLineItem({\n  image = null,\n  name = '',\n  modifiers = null,     // e.g. \"Large, extra cheese\"\n  price = 0,\n  qty = 1,\n  onQtyChange = null,   // null => read-only display\n  onRemove = null,\n}) {\n  return (\n    <div style={{\n      display: 'flex', alignItems: 'center', gap: 'var(--space-md)', padding: 'var(--space-md) 0',\n      fontFamily: 'var(--font-family-base)',\n    }}>\n      <div style={{\n        width: 56, height: 56, flex: '0 0 auto', borderRadius: 'var(--radius-sm)', overflow: 'hidden',\n        background: image ? `center/cover no-repeat url(\"${image}\")` : 'var(--color-bg-surface-sunken)',\n      }} />\n      <div style={{ flex: 1, minWidth: 0 }}>\n        <div style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{name}</div>\n        {modifiers && <div style={{ fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{modifiers}</div>}\n        <div style={{ fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)', marginTop: 4, fontVariantNumeric: 'tabular-nums' }}>\n          ${(price * qty).toFixed(2)}\n        </div>\n      </div>\n      <div style={{ flex: '0 0 auto', display: 'flex', flexDirection: 'column', alignItems: 'flex-end', gap: 6 }}>\n        {onQtyChange ? (\n          <Stepper size=\"sm\" value={qty} min={0} onChange={onQtyChange} />\n        ) : (\n          <span style={{ fontSize: 'var(--font-size-body)', color: 'var(--color-text-tertiary)' }}>×{qty}</span>\n        )}\n        {onRemove && (\n          <button type=\"button\" onClick={onRemove} aria-label=\"Remove item\" style={{\n            border: 'none', background: 'transparent', color: 'var(--color-text-tertiary)', fontSize: 12, cursor: 'pointer', padding: 0,\n          }}>Remove</button>\n        )}\n      </div>\n    </div>\n  );\n}\n","components/menu/ItemCustomizationSheet.jsx":"import React from 'react';\nimport { BottomSheet } from '../feedback/BottomSheet.jsx';\nimport { RadioGroup } from '../forms/Radio.jsx';\nimport { Checkbox } from '../forms/Checkbox.jsx';\nimport { Stepper } from '../forms/Stepper.jsx';\nimport { Button } from '../buttons/Button.jsx';\n\n/**\n * ItemCustomizationSheet — bottom sheet for configuring a menu item before\n * adding to cart: single-choice option groups (size), multi-choice add-ons,\n * quantity and special instructions. Composes BottomSheet + form controls.\n */\nexport function ItemCustomizationSheet({\n  open = false,\n  onClose = () => {},\n  itemName = '',\n  basePrice = 0,\n  sizeOptions = null,      // [{label,value,priceDelta}]\n  addOns = [],             // [{key,label,price}]\n  onAddToCart = () => {},\n}) {\n  const [size, setSize] = React.useState(sizeOptions ? sizeOptions[0].value : null);\n  const [selected, setSelected] = React.useState({});\n  const [qty, setQty] = React.useState(1);\n\n  const sizeDelta = sizeOptions ? (sizeOptions.find((s) => s.value === size)?.priceDelta || 0) : 0;\n  const addOnsTotal = addOns.reduce((sum, a) => sum + (selected[a.key] ? a.price : 0), 0);\n  const total = (basePrice + sizeDelta + addOnsTotal) * qty;\n\n  return (\n    <BottomSheet open={open} onClose={onClose} title={itemName}\n      footer={\n        <div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-md)' }}>\n          <Stepper value={qty} min={1} max={10} onChange={setQty} />\n          <Button fullWidth onClick={() => onAddToCart({ size, addOns: selected, qty, total })}>\n            Add to cart · ${total.toFixed(2)}\n          </Button>\n        </div>\n      }\n    >\n      <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-lg)' }}>\n        {sizeOptions && (\n          <div>\n            <h3 style={{ margin: '0 0 var(--space-md)', fontSize: 'var(--font-size-h3)', color: 'var(--color-text-primary)' }}>Size</h3>\n            <RadioGroup name=\"size\" value={size} onChange={setSize}\n              options={sizeOptions.map((s) => ({ label: s.priceDelta ? `${s.label} (+$${s.priceDelta.toFixed(2)})` : s.label, value: s.value }))} />\n          </div>\n        )}\n        {addOns.length > 0 && (\n          <div>\n            <h3 style={{ margin: '0 0 var(--space-md)', fontSize: 'var(--font-size-h3)', color: 'var(--color-text-primary)' }}>Add-ons</h3>\n            <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-sm)' }}>\n              {addOns.map((a) => (\n                <div key={a.key} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>\n                  <Checkbox checked={!!selected[a.key]} label={a.label}\n                    onChange={(v) => setSelected((s) => ({ ...s, [a.key]: v }))} />\n                  <span style={{ fontSize: 'var(--font-size-body)', color: 'var(--color-text-tertiary)' }}>+${a.price.toFixed(2)}</span>\n                </div>\n              ))}\n            </div>\n          </div>\n        )}\n      </div>\n    </BottomSheet>\n  );\n}\n","components/menu/MenuItemCard.jsx":"import React from 'react';\nimport { Stepper } from '../forms/Stepper.jsx';\n\n/**\n * MenuItemCard — dish photo, name, short description, price and an\n * add-to-cart control. Horizontal list-style layout (text left,\n * image right) common to food ordering apps.\n *\n * When quantity is 0 an \"Add\" button shows; once > 0 it becomes a\n * compact Stepper.\n */\nexport function MenuItemCard({\n  name = 'Dish name',\n  description = '',\n  price = 0,\n  currency = '$',\n  imageUrl = null,\n  tag = null,\n  quantity = 0,\n  onAdd = () => {},\n  onQuantityChange = () => {},\n}) {\n  const [addPressed, setAddPressed] = React.useState(false);\n\n  const fmt = (n) =>\n    `${currency}${Number(n).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;\n\n  const card = {\n    display: 'flex',\n    gap: 'var(--space-base)',\n    padding: 'var(--space-base)',\n    background: 'var(--card-bg)',\n    border: 'var(--border-width-default) solid var(--card-border)',\n    borderRadius: 'var(--radius-card)',\n    boxShadow: 'var(--elevation-card)',\n    fontFamily: 'var(--font-family-base)',\n    alignItems: 'stretch',\n  };\n\n  const addBtn = {\n    position: 'absolute',\n    right: 8,\n    bottom: 8,\n    width: 36,\n    height: 36,\n    display: 'inline-flex',\n    alignItems: 'center',\n    justifyContent: 'center',\n    border: 'none',\n    borderRadius: 'var(--radius-md)',\n    background: addPressed ? 'var(--button-primary-bg-active)' : 'var(--button-primary-bg)',\n    color: 'var(--button-primary-text)',\n    boxShadow: 'var(--elevation-raised)',\n    cursor: 'pointer',\n    fontSize: 'var(--icon-md)',\n    transition: 'var(--transition-press)',\n    WebkitTapHighlightColor: 'transparent',\n  };\n\n  return (\n    <div style={card}>\n      {/* Text column */}\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', minWidth: 0 }}>\n        {tag && (\n          <span\n            style={{\n              alignSelf: 'flex-start',\n              marginBottom: 'var(--space-sm)',\n              padding: '2px 8px',\n              background: 'var(--color-brand-subtle)',\n              color: 'var(--color-text-accent)',\n              borderRadius: 'var(--radius-tag)',\n              fontSize: 'var(--font-size-overline)',\n              lineHeight: 'var(--line-height-overline)',\n              fontWeight: 'var(--font-weight-semibold)',\n              letterSpacing: 'var(--letter-spacing-overline)',\n              textTransform: 'uppercase',\n            }}\n          >\n            {tag}\n          </span>\n        )}\n        <div\n          style={{\n            fontSize: 'var(--font-size-h3)',\n            lineHeight: 'var(--line-height-h3)',\n            fontWeight: 'var(--font-weight-semibold)',\n            color: 'var(--color-text-primary)',\n          }}\n        >\n          {name}\n        </div>\n        {description && (\n          <div\n            style={{\n              marginTop: 'var(--space-xs)',\n              fontSize: 'var(--font-size-body)',\n              lineHeight: 'var(--line-height-body)',\n              color: 'var(--color-text-secondary)',\n              display: '-webkit-box',\n              WebkitLineClamp: 2,\n              WebkitBoxOrient: 'vertical',\n              overflow: 'hidden',\n            }}\n          >\n            {description}\n          </div>\n        )}\n        <div\n          style={{\n            marginTop: 'auto',\n            paddingTop: 'var(--space-md)',\n            fontSize: 'var(--font-size-h3)',\n            lineHeight: 'var(--line-height-h3)',\n            fontWeight: 'var(--font-weight-bold)',\n            color: 'var(--color-text-primary)',\n            fontVariantNumeric: 'tabular-nums',\n          }}\n        >\n          {fmt(price)}\n        </div>\n      </div>\n\n      {/* Image + action */}\n      <div style={{ position: 'relative', flex: '0 0 104px', width: 104 }}>\n        <div\n          style={{\n            width: 104,\n            height: 104,\n            borderRadius: 'var(--radius-md)',\n            background: imageUrl\n              ? `center / cover no-repeat url(\"${imageUrl}\")`\n              : 'var(--color-bg-surface-sunken)',\n            display: 'flex',\n            alignItems: 'center',\n            justifyContent: 'center',\n            color: 'var(--color-text-disabled)',\n            fontSize: 32,\n          }}\n        >\n          {!imageUrl && <i className=\"ph ph-image\" aria-hidden=\"true\" />}\n        </div>\n\n        {quantity > 0 ? (\n          <div style={{ position: 'absolute', right: 0, bottom: 8 }}>\n            <Stepper size=\"sm\" value={quantity} min={0} onChange={onQuantityChange} ariaLabel={`Quantity of ${name}`} />\n          </div>\n        ) : (\n          <button\n            type=\"button\"\n            style={addBtn}\n            onClick={onAdd}\n            onPointerDown={() => setAddPressed(true)}\n            onPointerUp={() => setAddPressed(false)}\n            onPointerLeave={() => setAddPressed(false)}\n            aria-label={`Add ${name}`}\n          >\n            <i className=\"ph ph-plus\" aria-hidden=\"true\" />\n          </button>\n        )}\n      </div>\n    </div>\n  );\n}\n","components/navigation/BottomTabBar.jsx":"import React from 'react';\n\n/**\n * BottomTabBar — persistent bottom navigation between core sections.\n * 56px (no labels) to 64px (labeled). Active item uses accent + filled icon.\n */\nexport function BottomTabBar({\n  items = [],          // [{ key, icon, label, badge }]\n  activeKey = null,\n  onChange = () => {},\n  showLabels = true,\n}) {\n  const bar = {\n    display: 'flex', alignItems: 'stretch',\n    height: showLabels ? 'var(--bottomnav-height-labeled)' : 'var(--bottomnav-height)',\n    background: 'var(--bottomnav-bg)',\n    borderTop: 'var(--border-width-default) solid var(--appbar-border)',\n    paddingBottom: 'env(safe-area-inset-bottom, 0px)',\n    fontFamily: 'var(--font-family-base)',\n  };\n\n  return (\n    <nav style={bar} aria-label=\"Primary\">\n      {items.map((it) => {\n        const active = it.key === activeKey;\n        return (\n          <button\n            key={it.key} type=\"button\" onClick={() => onChange(it.key)}\n            aria-current={active ? 'page' : undefined}\n            style={{\n              flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center',\n              justifyContent: 'center', gap: 2, border: 'none', background: 'transparent',\n              color: active ? 'var(--bottomnav-icon-active)' : 'var(--bottomnav-icon)',\n              cursor: 'pointer', padding: '6px 0', WebkitTapHighlightColor: 'transparent',\n              transition: 'color var(--duration-fast) var(--ease-standard)',\n            }}\n          >\n            <span style={{ position: 'relative', display: 'inline-flex' }}>\n              <i className={`${active ? 'ph-fill' : 'ph'} ph-${it.icon}`} aria-hidden=\"true\" style={{ fontSize: 'var(--icon-md)' }} />\n              {it.badge != null && (\n                <span style={{\n                  position: 'absolute', top: -4, right: -8, minWidth: 16, height: 16, padding: '0 4px',\n                  boxSizing: 'border-box', display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n                  background: 'var(--badge-bg)', color: 'var(--badge-text)', borderRadius: 'var(--radius-full)',\n                  fontSize: 10, fontWeight: 'var(--font-weight-bold)', lineHeight: 1,\n                }}>{it.badge}</span>\n              )}\n            </span>\n            {showLabels && (\n              <span style={{\n                fontSize: 'var(--font-size-overline)', lineHeight: 'var(--line-height-overline)',\n                fontWeight: active ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n              }}>{it.label}</span>\n            )}\n          </button>\n        );\n      })}\n    </nav>\n  );\n}\n","components/navigation/Breadcrumb.jsx":"import React from 'react';\n\n/**\n * Breadcrumb — current location within a multi-step flow (e.g. checkout).\n * Steps show completed / current / upcoming states with a connector.\n */\nexport function Breadcrumb({\n  steps = [],          // [{ label }]\n  current = 0,\n  onStepClick = null,  // (index) => void — only completed steps clickable\n}) {\n  return (\n    <nav aria-label=\"Progress\" style={{ fontFamily: 'var(--font-family-base)' }}>\n      <ol style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', listStyle: 'none', margin: 0, padding: 0 }}>\n        {steps.map((s, i) => {\n          const done = i < current;\n          const active = i === current;\n          const clickable = done && onStepClick;\n          return (\n            <li key={i} style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)' }}>\n              <button\n                type=\"button\" disabled={!clickable}\n                onClick={() => clickable && onStepClick(i)}\n                style={{\n                  display: 'inline-flex', alignItems: 'center', gap: 6, border: 'none',\n                  background: 'transparent', padding: 0, cursor: clickable ? 'pointer' : 'default',\n                }}\n                aria-current={active ? 'step' : undefined}\n              >\n                <span style={{\n                  width: 22, height: 22, flex: '0 0 auto', display: 'inline-flex', alignItems: 'center',\n                  justifyContent: 'center', borderRadius: '50%', fontSize: 12, fontWeight: 'var(--font-weight-bold)',\n                  background: done ? 'var(--color-brand-primary)' : active ? 'var(--color-brand-subtle)' : 'var(--color-bg-surface-sunken)',\n                  color: done ? 'var(--color-text-on-brand)' : active ? 'var(--color-text-accent)' : 'var(--color-text-tertiary)',\n                  border: active ? '2px solid var(--color-brand-primary)' : 'none', boxSizing: 'border-box',\n                }}>\n                  {done ? <i className=\"ph-bold ph-check\" aria-hidden=\"true\" /> : i + 1}\n                </span>\n                <span style={{\n                  fontSize: 'var(--font-size-body)',\n                  fontWeight: active ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n                  color: active ? 'var(--color-text-primary)' : 'var(--color-text-tertiary)',\n                }}>{s.label}</span>\n              </button>\n              {i < steps.length - 1 && (\n                <i className=\"ph ph-caret-right\" aria-hidden=\"true\" style={{ fontSize: 14, color: 'var(--color-text-tertiary)' }} />\n              )}\n            </li>\n          );\n        })}\n      </ol>\n    </nav>\n  );\n}\n","components/navigation/NavigationDrawer.jsx":"import React from 'react';\n\n/**\n * NavigationDrawer — slide-out panel from the left for secondary /\n * account-level navigation. Includes a scrim; closes on scrim tap.\n */\nexport function NavigationDrawer({\n  open = false,\n  onClose = () => {},\n  header = null,\n  items = [],          // [{ icon, label, key, active }]\n  onSelect = () => {},\n  footer = null,\n}) {\n  return (\n    <div aria-hidden={!open} style={{ position: 'absolute', inset: 0, pointerEvents: open ? 'auto' : 'none', zIndex: 40 }}>\n      <div\n        onClick={onClose}\n        style={{\n          position: 'absolute', inset: 0, background: 'var(--color-bg-scrim)',\n          opacity: open ? 1 : 0, transition: 'opacity var(--duration-base) var(--ease-standard)',\n        }}\n      />\n      <aside\n        role=\"dialog\" aria-label=\"Navigation\"\n        style={{\n          position: 'absolute', top: 0, bottom: 0, left: 0, width: 296, maxWidth: '85%',\n          background: 'var(--color-bg-main)', boxShadow: 'var(--elevation-overlay)',\n          transform: open ? 'translateX(0)' : 'translateX(-100%)',\n          transition: 'transform var(--duration-slow) var(--ease-decelerate)',\n          display: 'flex', flexDirection: 'column', fontFamily: 'var(--font-family-base)',\n          padding: 'var(--space-base)', boxSizing: 'border-box',\n        }}\n      >\n        {header && <div style={{ padding: 'var(--space-sm) var(--space-sm) var(--space-base)' }}>{header}</div>}\n        <nav style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 2 }}>\n          {items.map((it) => (\n            <button\n              key={it.key} type=\"button\" onClick={() => onSelect(it.key)}\n              aria-current={it.active ? 'page' : undefined}\n              style={{\n                display: 'flex', alignItems: 'center', gap: 'var(--space-md)', minHeight: 48,\n                padding: '0 var(--space-md)', border: 'none', borderRadius: 'var(--radius-md)',\n                background: it.active ? 'var(--color-brand-subtle)' : 'transparent',\n                color: it.active ? 'var(--color-text-accent)' : 'var(--color-text-primary)',\n                fontSize: 'var(--font-size-body-lg)', fontWeight: it.active ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n                cursor: 'pointer', textAlign: 'left', WebkitTapHighlightColor: 'transparent',\n              }}\n            >\n              <i className={`${it.active ? 'ph-fill' : 'ph'} ph-${it.icon}`} aria-hidden=\"true\" style={{ fontSize: 'var(--icon-md)' }} />\n              {it.label}\n            </button>\n          ))}\n        </nav>\n        {footer && <div style={{ paddingTop: 'var(--space-base)', borderTop: 'var(--border-width-default) solid var(--color-border-muted)' }}>{footer}</div>}\n      </aside>\n    </div>\n  );\n}\n","components/navigation/PageIndicator.jsx":"import React from 'react';\n\n/**\n * PageIndicator (Pagination) — dots showing position within a carousel or\n * multi-step flow. Active dot elongates to a pill.\n */\nexport function PageIndicator({ count = 3, activeIndex = 0, onDotClick = null }) {\n  return (\n    <div style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }} role=\"tablist\" aria-label=\"Pages\">\n      {Array.from({ length: count }).map((_, i) => {\n        const active = i === activeIndex;\n        const clickable = Boolean(onDotClick);\n        return (\n          <button\n            key={i} type=\"button\" role=\"tab\" aria-selected={active}\n            aria-label={`Page ${i + 1}`} disabled={!clickable}\n            onClick={() => clickable && onDotClick(i)}\n            style={{\n              width: active ? 20 : 8, height: 8, padding: 0, border: 'none',\n              borderRadius: 'var(--radius-full)',\n              background: active ? 'var(--color-brand-primary)' : 'var(--color-border-strong)',\n              cursor: clickable ? 'pointer' : 'default',\n              transition: 'width var(--duration-base) var(--ease-standard), background-color var(--duration-base) var(--ease-standard)',\n            }}\n          />\n        );\n      })}\n    </div>\n  );\n}\n","components/navigation/TopAppBar.jsx":"import React from 'react';\n\n/**\n * TopAppBar — screen header with title, optional back button and trailing\n * action icons (search, cart, notifications). 56px tall (Android standard).\n */\nexport function TopAppBar({\n  title = '',\n  subtitle = null,\n  onBack = null,\n  actions = [],        // [{ icon, onClick, ariaLabel, badge }]\n  centerTitle = false,\n  elevated = false,\n}) {\n  const bar = {\n    display: 'flex', alignItems: 'center', gap: 'var(--space-sm)',\n    height: 'var(--appbar-height)', padding: '0 var(--space-sm)',\n    background: 'var(--appbar-bg)',\n    borderBottom: elevated ? 'none' : 'var(--border-width-default) solid var(--appbar-border)',\n    boxShadow: elevated ? 'var(--elevation-card)' : 'none',\n    fontFamily: 'var(--font-family-base)',\n  };\n  const iconBtn = {\n    width: 48, height: 48, flex: '0 0 auto', display: 'inline-flex',\n    alignItems: 'center', justifyContent: 'center', border: 'none',\n    background: 'transparent', color: 'var(--color-text-primary)',\n    borderRadius: 'var(--radius-full)', cursor: 'pointer', fontSize: 'var(--icon-md)',\n    position: 'relative', WebkitTapHighlightColor: 'transparent',\n  };\n\n  return (\n    <header style={bar}>\n      {onBack && (\n        <button type=\"button\" style={iconBtn} onClick={onBack} aria-label=\"Back\">\n          <i className=\"ph ph-arrow-left\" aria-hidden=\"true\" />\n        </button>\n      )}\n      <div style={{\n        flex: 1, minWidth: 0, paddingLeft: onBack ? 0 : 'var(--space-sm)',\n        textAlign: centerTitle ? 'center' : 'left',\n      }}>\n        <div style={{\n          fontSize: 'var(--font-size-h3)', lineHeight: 'var(--line-height-h3)',\n          fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)',\n          overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',\n        }}>{title}</div>\n        {subtitle && (\n          <div style={{\n            fontSize: 'var(--font-size-caption)', lineHeight: 'var(--line-height-caption)',\n            color: 'var(--color-text-tertiary)',\n          }}>{subtitle}</div>\n        )}\n      </div>\n      {actions.map((a, i) => (\n        <button key={i} type=\"button\" style={iconBtn} onClick={a.onClick} aria-label={a.ariaLabel}>\n          <i className={`ph ph-${a.icon}`} aria-hidden=\"true\" />\n          {a.badge != null && (\n            <span style={{\n              position: 'absolute', top: 6, right: 6, minWidth: 16, height: 16, padding: '0 4px',\n              boxSizing: 'border-box', display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n              background: 'var(--badge-bg)', color: 'var(--badge-text)', borderRadius: 'var(--radius-full)',\n              fontSize: 10, fontWeight: 'var(--font-weight-bold)', lineHeight: 1,\n            }}>{a.badge}</span>\n          )}\n        </button>\n      ))}\n    </header>\n  );\n}\n","components/news/NewsListing.jsx":"import React from 'react';\n\n/**\n * NewsListing — image + title + publish date item for restaurant news,\n * announcements or blog posts. `orientation=\"vertical\"` stacks image on\n * top (grid feeds); `orientation=\"horizontal\"` places a thumbnail beside\n * the text (list feeds).\n */\nexport function NewsListing({\n  image = null,\n  title = '',\n  publishedAt = '',\n  orientation = 'vertical',\n  onClick = null,\n}) {\n  const interactive = Boolean(onClick);\n  const Tag = interactive ? 'button' : 'div';\n  const isHorizontal = orientation === 'horizontal';\n\n  const thumb = {\n    flex: isHorizontal ? '0 0 auto' : '0 0 auto',\n    width: isHorizontal ? 96 : '100%',\n    height: isHorizontal ? 96 : 140,\n    borderRadius: 'var(--radius-md)', overflow: 'hidden',\n    background: image ? `center/cover no-repeat url(\"${image}\")` : 'var(--color-bg-surface-sunken)',\n  };\n\n  return (\n    <Tag\n      type={interactive ? 'button' : undefined} onClick={onClick}\n      style={{\n        display: 'flex', flexDirection: isHorizontal ? 'row' : 'column',\n        alignItems: isHorizontal ? 'center' : 'stretch', gap: 'var(--space-md)',\n        width: isHorizontal ? '100%' : 220, boxSizing: 'border-box', textAlign: 'left',\n        background: 'transparent', border: 'none', padding: 0, font: 'inherit',\n        cursor: interactive ? 'pointer' : 'default', fontFamily: 'var(--font-family-base)',\n      }}\n    >\n      <div style={thumb} />\n      <div style={{ flex: 1, minWidth: 0, display: 'flex', flexDirection: 'column', gap: 4 }}>\n        <span style={{\n          fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)',\n          display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden',\n        }}>{title}</span>\n        <span style={{ fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)' }}>{publishedAt}</span>\n      </div>\n    </Tag>\n  );\n}\n","components/order/LiveOrderTrackingMap.jsx":"import React from 'react';\nimport { MapView } from '../media/MapView.jsx';\n\n/**\n * LiveOrderTrackingMap — MapView preset showing a live courier position\n * with an ETA caption. Thin composition over MapView for order-tracking\n * screens specifically.\n */\nexport function LiveOrderTrackingMap({ eta = '', height = 220 }) {\n  return <MapView height={height} showCourier markerLabel={eta ? `Arriving in ${eta}` : null} />;\n}\n","components/order/OrderHistoryCard.jsx":"import React from 'react';\n\n/**\n * OrderHistoryCard — past-order summary: restaurant, date, item preview,\n * total and status. Composes with ReorderButton as a trailing action.\n */\nexport function OrderHistoryCard({\n  restaurantName = '',\n  date = '',\n  itemsSummary = '',\n  total = 0,\n  status = 'Delivered',\n  onClick = null,\n  actions = null,       // node, e.g. <ReorderButton onClick={...} />\n}) {\n  return (\n    <div onClick={onClick} style={{\n      padding: 'var(--space-base)', background: 'var(--card-bg)', border: 'var(--border-width-default) solid var(--card-border)',\n      borderRadius: 'var(--radius-card)', boxShadow: 'var(--elevation-card)', cursor: onClick ? 'pointer' : 'default',\n      fontFamily: 'var(--font-family-base)',\n    }}>\n      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', gap: 8 }}>\n        <div>\n          <div style={{ fontSize: 'var(--font-size-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>{restaurantName}</div>\n          <div style={{ fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)' }}>{date}</div>\n        </div>\n        <span style={{\n          fontSize: 11, fontWeight: 'var(--font-weight-bold)', padding: '4px 10px', borderRadius: 'var(--radius-full)',\n          background: 'var(--color-bg-success)', color: 'var(--color-text-success)', whiteSpace: 'nowrap',\n        }}>{status}</span>\n      </div>\n      <div style={{ marginTop: 'var(--space-sm)', fontSize: 'var(--font-size-body)', color: 'var(--color-text-secondary)' }}>{itemsSummary}</div>\n      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: 'var(--space-md)' }}>\n        <span style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-bold)', color: 'var(--color-text-primary)', fontVariantNumeric: 'tabular-nums' }}>${total.toFixed(2)}</span>\n        {actions}\n      </div>\n    </div>\n  );\n}\n","components/order/OrderStatusTimeline.jsx":"import React from 'react';\n\n/**\n * OrderStatusTimeline — vertical stepper showing order progress\n * (Placed → Preparing → Out for delivery → Delivered). Completed steps\n * fill with accent + check; current step pulses.\n */\nexport function OrderStatusTimeline({ steps = [], currentIndex = 0 }) {\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      {steps.map((s, i) => {\n        const done = i < currentIndex;\n        const active = i === currentIndex;\n        const last = i === steps.length - 1;\n        return (\n          <div key={i} style={{ display: 'flex', gap: 'var(--space-md)' }}>\n            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>\n              <span style={{\n                width: 28, height: 28, borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center',\n                background: done || active ? 'var(--color-brand-primary)' : 'var(--color-bg-surface-sunken)',\n                color: done || active ? 'var(--color-text-on-brand)' : 'var(--color-text-tertiary)',\n                boxShadow: active ? '0 0 0 4px var(--accent-100)' : 'none', flex: '0 0 auto',\n              }}>\n                {done ? <i className=\"ph-bold ph-check\" aria-hidden=\"true\" style={{ fontSize: 14 }} /> : <i className={`ph ph-${s.icon || 'circle'}`} aria-hidden=\"true\" style={{ fontSize: 14 }} />}\n              </span>\n              {!last && <span style={{ width: 2, flex: 1, minHeight: 28, background: done ? 'var(--color-brand-primary)' : 'var(--color-border-muted)' }} />}\n            </div>\n            <div style={{ paddingBottom: last ? 0 : 'var(--space-lg)' }}>\n              <div style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: active ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)', color: done || active ? 'var(--color-text-primary)' : 'var(--color-text-tertiary)' }}>{s.label}</div>\n              {s.time && <div style={{ fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)' }}>{s.time}</div>}\n            </div>\n          </div>\n        );\n      })}\n    </div>\n  );\n}\n","components/order/RatingReviewPrompt.jsx":"import React from 'react';\nimport { RatingStars } from '../rating/RatingStars.jsx';\nimport { Button } from '../buttons/Button.jsx';\n\n/**\n * RatingReviewPrompt — post-delivery card asking the user to rate their\n * order and optionally leave a written review.\n */\nexport function RatingReviewPrompt({\n  restaurantName = '',\n  rating = 0,\n  onRatingChange = () => {},\n  review = '',\n  onReviewChange = () => {},\n  onSubmit = () => {},\n  onSkip = () => {},\n}) {\n  return (\n    <div style={{\n      padding: 'var(--space-lg)', background: 'var(--card-bg)', border: 'var(--border-width-default) solid var(--card-border)',\n      borderRadius: 'var(--radius-card)', boxShadow: 'var(--elevation-card)', textAlign: 'center', fontFamily: 'var(--font-family-base)',\n    }}>\n      <h3 style={{ margin: 0, fontSize: 'var(--font-size-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>How was {restaurantName}?</h3>\n      <div style={{ display: 'flex', justifyContent: 'center', marginTop: 'var(--space-md)' }}>\n        <RatingStars value={rating} readOnly={false} onChange={onRatingChange} size={30} />\n      </div>\n      <textarea\n        value={review} onChange={(e) => onReviewChange(e.target.value)} placeholder=\"Add a comment (optional)\"\n        rows={2} style={{\n          width: '100%', marginTop: 'var(--space-md)', boxSizing: 'border-box', padding: 'var(--space-md)',\n          border: 'var(--border-width-default) solid var(--color-border-default)', borderRadius: 'var(--radius-input)',\n          fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)', resize: 'vertical', outline: 'none',\n        }}\n      />\n      <div style={{ display: 'flex', gap: 'var(--space-md)', marginTop: 'var(--space-md)' }}>\n        <Button variant=\"tertiary\" onClick={onSkip} fullWidth>Skip</Button>\n        <Button onClick={onSubmit} fullWidth disabled={rating === 0}>Submit</Button>\n      </div>\n    </div>\n  );\n}\n","components/order/ReorderButton.jsx":"import React from 'react';\n\n/**\n * ReorderButton — compact secondary action to repeat a past order.\n * Meant as a trailing action inside OrderHistoryCard or similar rows.\n */\nexport function ReorderButton({ onClick = () => {}, label = 'Reorder' }) {\n  const [pressed, setPressed] = React.useState(false);\n  return (\n    <button type=\"button\" onClick={onClick}\n      onPointerDown={() => setPressed(true)} onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n      style={{\n        display: 'inline-flex', alignItems: 'center', gap: 6, height: 36, padding: '0 var(--space-md)',\n        borderRadius: 'var(--radius-full)', border: 'var(--border-width-default) solid var(--color-border-default)',\n        background: pressed ? 'var(--color-bg-surface-sunken)' : 'transparent', color: 'var(--color-text-primary)',\n        fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)',\n        cursor: 'pointer', transition: 'var(--transition-press)', WebkitTapHighlightColor: 'transparent',\n      }}>\n      <i className=\"ph ph-arrow-clockwise\" aria-hidden=\"true\" style={{ fontSize: 16 }} />{label}\n    </button>\n  );\n}\n","components/pickers/Calendar.jsx":"import React from 'react';\n\n/**\n * Calendar — full month-grid date picker combined with time-slot selection,\n * for choosing a date AND time in one surface (scheduled orders,\n * reservations). Navigate months with the arrows; pick a day, then a time.\n */\nexport function Calendar({\n  value = null,          // { date: 'YYYY-MM-DD', time: 'HH:mm' }\n  onChange = () => {},\n  timeSlots = ['12:00', '12:30', '13:00', '18:00', '18:30', '19:00', '19:30', '20:00'],\n  minDate = null,        // 'YYYY-MM-DD'\n}) {\n  const iso = (d) => `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;\n  const today = new Date(); today.setHours(0, 0, 0, 0);\n  const min = minDate ? new Date(minDate + 'T00:00:00') : today;\n\n  const selDate = value?.date || null;\n  const selTime = value?.time || null;\n  const [cursor, setCursor] = React.useState(() => selDate ? new Date(selDate + 'T00:00:00') : new Date(today));\n\n  const year = cursor.getFullYear();\n  const month = cursor.getMonth();\n  const firstWeekday = new Date(year, month, 1).getDay();\n  const daysInMonth = new Date(year, month + 1, 0).getDate();\n  const monthLabel = cursor.toLocaleDateString(undefined, { month: 'long', year: 'numeric' });\n\n  const cells = [];\n  for (let i = 0; i < firstWeekday; i++) cells.push(null);\n  for (let d = 1; d <= daysInMonth; d++) cells.push(new Date(year, month, d));\n\n  const emit = (patch) => onChange({ date: selDate, time: selTime, ...patch });\n\n  const navBtn = {\n    width: 36, height: 36, display: 'inline-flex', alignItems: 'center', justifyContent: 'center',\n    border: 'none', background: 'transparent', borderRadius: 'var(--radius-full)',\n    color: 'var(--color-text-secondary)', cursor: 'pointer', fontSize: 18, WebkitTapHighlightColor: 'transparent',\n  };\n  const wd = ['S', 'M', 'T', 'W', 'T', 'F', 'S'];\n\n  return (\n    <div style={{\n      width: 300, boxSizing: 'border-box', padding: 'var(--space-base)', background: 'var(--color-bg-surface)',\n      border: 'var(--border-width-default) solid var(--color-border-muted)', borderRadius: 'var(--radius-card)',\n      boxShadow: 'var(--elevation-card)', fontFamily: 'var(--font-family-base)',\n    }}>\n      {/* Month header */}\n      <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 'var(--space-sm)' }}>\n        <button type=\"button\" style={navBtn} aria-label=\"Previous month\" onClick={() => setCursor(new Date(year, month - 1, 1))}>\n          <i className=\"ph ph-caret-left\" aria-hidden=\"true\" />\n        </button>\n        <span style={{ fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)' }}>{monthLabel}</span>\n        <button type=\"button\" style={navBtn} aria-label=\"Next month\" onClick={() => setCursor(new Date(year, month + 1, 1))}>\n          <i className=\"ph ph-caret-right\" aria-hidden=\"true\" />\n        </button>\n      </div>\n\n      {/* Weekday header */}\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 1fr)', gap: 2, marginBottom: 4 }}>\n        {wd.map((w, i) => (\n          <span key={i} style={{ textAlign: 'center', fontSize: 'var(--font-size-overline)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-tertiary)' }}>{w}</span>\n        ))}\n      </div>\n\n      {/* Day grid */}\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 1fr)', gap: 2 }}>\n        {cells.map((d, i) => {\n          if (!d) return <span key={i} />;\n          const key = iso(d);\n          const disabled = d < min;\n          const active = key === selDate;\n          const isToday = key === iso(today);\n          return (\n            <button key={i} type=\"button\" disabled={disabled} onClick={() => emit({ date: key })}\n              aria-pressed={active} aria-label={d.toLocaleDateString()}\n              style={{\n                height: 36, border: 'none', borderRadius: 'var(--radius-full)', cursor: disabled ? 'not-allowed' : 'pointer',\n                background: active ? 'var(--color-brand-primary)' : 'transparent',\n                color: active ? 'var(--color-text-on-brand)' : disabled ? 'var(--color-text-disabled)' : 'var(--color-text-primary)',\n                fontSize: 'var(--font-size-body)', fontWeight: active || isToday ? 'var(--font-weight-semibold)' : 'var(--font-weight-regular)',\n                opacity: disabled ? 0.5 : 1, WebkitTapHighlightColor: 'transparent', position: 'relative',\n                transition: 'var(--transition-colors)',\n              }}>\n              {d.getDate()}\n              {isToday && !active && (\n                <span aria-hidden=\"true\" style={{ position: 'absolute', bottom: 4, left: '50%', transform: 'translateX(-50%)', width: 4, height: 4, borderRadius: '50%', background: 'var(--color-brand-primary)' }} />\n              )}\n            </button>\n          );\n        })}\n      </div>\n\n      {/* Time slots */}\n      <div style={{ marginTop: 'var(--space-base)', paddingTop: 'var(--space-md)', borderTop: 'var(--border-width-default) solid var(--color-border-muted)' }}>\n        <div style={{ fontSize: 'var(--font-size-overline)', letterSpacing: 'var(--letter-spacing-overline)', textTransform: 'uppercase', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-tertiary)', marginBottom: 'var(--space-sm)' }}>Time</div>\n        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6 }}>\n          {timeSlots.map((s) => {\n            const slot = typeof s === 'string' ? { time: s, disabled: false } : s;\n            const active = selTime === slot.time;\n            return (\n              <button key={slot.time} type=\"button\" disabled={slot.disabled} onClick={() => emit({ time: slot.time })}\n                style={{\n                  minWidth: 60, height: 34, padding: '0 var(--space-sm)', borderRadius: 'var(--radius-full)',\n                  border: active ? 'none' : 'var(--border-width-default) solid var(--color-border-default)',\n                  background: active ? 'var(--color-brand-primary)' : 'var(--color-bg-main)',\n                  color: active ? 'var(--color-text-on-brand)' : 'var(--color-text-primary)',\n                  opacity: slot.disabled ? 'var(--state-disabled-opacity)' : 1, cursor: slot.disabled ? 'not-allowed' : 'pointer',\n                  fontSize: 'var(--font-size-caption)', fontWeight: 'var(--font-weight-semibold)', WebkitTapHighlightColor: 'transparent',\n                  transition: 'var(--transition-colors)',\n                }}>{slot.time}</button>\n            );\n          })}\n        </div>\n      </div>\n    </div>\n  );\n}\n","components/pickers/DatePicker.jsx":"import React from 'react';\n\n/**\n * DatePicker — compact field opening a horizontal day-scroller for\n * near-term dates (reservations, scheduled orders). Not a full calendar.\n */\nexport function DatePicker({ value = null, onChange = () => {}, daysAhead = 14, label = 'Date' }) {\n  const today = new Date(); today.setHours(0, 0, 0, 0);\n  const days = Array.from({ length: daysAhead }, (_, i) => {\n    const d = new Date(today); d.setDate(d.getDate() + i); return d;\n  });\n  const fmt = (d) => d.toISOString().slice(0, 10);\n  const selected = value || fmt(days[0]);\n\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      {label && (\n        <div style={{ marginBottom: 'var(--space-sm)', fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-medium)', color: 'var(--input-label)' }}>{label}</div>\n      )}\n      <div style={{ display: 'flex', gap: 8, overflowX: 'auto', paddingBottom: 4 }}>\n        {days.map((d) => {\n          const key = fmt(d);\n          const active = key === selected;\n          const isToday = key === fmt(today);\n          return (\n            <button key={key} type=\"button\" onClick={() => onChange(key)} style={{\n              flex: '0 0 auto', width: 52, height: 64, display: 'flex', flexDirection: 'column', alignItems: 'center',\n              justifyContent: 'center', gap: 2, border: active ? 'none' : 'var(--border-width-default) solid var(--color-border-default)',\n              borderRadius: 'var(--radius-md)', background: active ? 'var(--color-brand-primary)' : 'var(--color-bg-main)',\n              color: active ? 'var(--color-text-on-brand)' : 'var(--color-text-primary)', cursor: 'pointer',\n              WebkitTapHighlightColor: 'transparent', transition: 'var(--transition-colors)',\n            }}>\n              <span style={{ fontSize: 11, fontWeight: 'var(--font-weight-semibold)', opacity: 0.8, textTransform: 'uppercase' }}>\n                {isToday ? 'Today' : d.toLocaleDateString(undefined, { weekday: 'short' })}\n              </span>\n              <span style={{ fontSize: 18, fontWeight: 'var(--font-weight-bold)' }}>{d.getDate()}</span>\n            </button>\n          );\n        })}\n      </div>\n    </div>\n  );\n}\n","components/pickers/TimePicker.jsx":"import React from 'react';\n\n/**\n * TimePicker — chip grid of available time slots (reservations, pickup\n * scheduling). Disabled slots represent unavailable times.\n */\nexport function TimePicker({ slots = [], value = null, onChange = () => {}, label = 'Time' }) {\n  return (\n    <div style={{ fontFamily: 'var(--font-family-base)' }}>\n      {label && (\n        <div style={{ marginBottom: 'var(--space-sm)', fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-medium)', color: 'var(--input-label)' }}>{label}</div>\n      )}\n      <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8 }}>\n        {slots.map((s) => {\n          const time = typeof s === 'string' ? { time: s, disabled: false } : s;\n          const active = value === time.time;\n          return (\n            <button key={time.time} type=\"button\" disabled={time.disabled}\n              onClick={() => onChange(time.time)}\n              style={{\n                minWidth: 72, height: 40, padding: '0 var(--space-md)', border: active ? 'none' : 'var(--border-width-default) solid var(--color-border-default)',\n                borderRadius: 'var(--radius-full)', background: active ? 'var(--color-brand-primary)' : 'var(--color-bg-main)',\n                color: active ? 'var(--color-text-on-brand)' : 'var(--color-text-primary)',\n                opacity: time.disabled ? 'var(--state-disabled-opacity)' : 1,\n                cursor: time.disabled ? 'not-allowed' : 'pointer', fontSize: 'var(--font-size-body)',\n                fontWeight: 'var(--font-weight-semibold)', WebkitTapHighlightColor: 'transparent',\n                transition: 'var(--transition-colors)',\n              }}>{time.time}</button>\n          );\n        })}\n      </div>\n    </div>\n  );\n}\n","components/quick-action-tiles/QuickActionTiles.jsx":"import React from 'react';\n\n/**\n * QuickActionTile — square tile with a centered icon and a label below,\n * for a row of shortcut actions (Rate, History, Favourites…).\n */\nexport function QuickActionTile({ icon = 'star', label = '', onClick = () => {} }) {\n  const [pressed, setPressed] = React.useState(false);\n  return (\n    <button type=\"button\" onClick={onClick}\n      onPointerDown={() => setPressed(true)} onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n      style={{\n        display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 'var(--space-sm)',\n        flex: 1, aspectRatio: '1 / 1', border: 'none', borderRadius: 'var(--radius-card)',\n        background: 'var(--card-bg)', boxShadow: 'var(--elevation-card)', cursor: 'pointer',\n        fontFamily: 'var(--font-family-base)', WebkitTapHighlightColor: 'transparent',\n        transform: pressed ? 'scale(var(--press-scale))' : 'none', transition: 'var(--transition-press)',\n      }}>\n      <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 28, color: 'var(--color-brand-primary)' }} />\n      <span style={{\n        fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-medium)', color: 'var(--color-text-primary)',\n      }}>{label}</span>\n    </button>\n  );\n}\n\n/**\n * QuickActionTiles — row of QuickActionTile shortcuts, evenly spaced.\n */\nexport function QuickActionTiles({ actions = [] }) {\n  return (\n    <div style={{ display: 'flex', gap: 'var(--space-md)' }}>\n      {actions.map((a) => (\n        <QuickActionTile key={a.key || a.label} icon={a.icon} label={a.label} onClick={a.onClick} />\n      ))}\n    </div>\n  );\n}\n","components/rating/RatingStars.jsx":"import React from 'react';\n\n/**\n * RatingStars — interactive or display-only star rating. Supports half\n * stars in display mode and an optional count label.\n */\nexport function RatingStars({\n  value = 0,\n  max = 5,\n  size = 20,\n  readOnly = true,\n  onChange = () => {},\n  count = null,        // e.g. 128 -> \"(128)\"\n}) {\n  const [hover, setHover] = React.useState(null);\n  const shown = hover != null ? hover : value;\n\n  const star = (i) => {\n    const full = shown >= i + 1;\n    const half = !full && shown >= i + 0.5;\n    const cls = full ? 'ph-fill ph-star' : half ? 'ph-fill ph-star-half' : 'ph ph-star';\n    return (\n      <i key={i} className={cls} aria-hidden=\"true\"\n        style={{ fontSize: size, color: full || half ? 'var(--amber-500)' : 'var(--color-border-strong)' }} />\n    );\n  };\n\n  if (readOnly) {\n    return (\n      <span style={{ display: 'inline-flex', alignItems: 'center', gap: 4, fontFamily: 'var(--font-family-base)' }}\n        role=\"img\" aria-label={`${value} out of ${max} stars`}>\n        <span style={{ display: 'inline-flex', gap: 2 }}>{Array.from({ length: max }).map((_, i) => star(i))}</span>\n        {count != null && <span style={{ fontSize: 'var(--font-size-body)', color: 'var(--color-text-tertiary)' }}>({count})</span>}\n      </span>\n    );\n  }\n\n  return (\n    <span style={{ display: 'inline-flex', gap: 4 }} role=\"radiogroup\" aria-label=\"Rating\">\n      {Array.from({ length: max }).map((_, i) => (\n        <button\n          key={i} type=\"button\" aria-label={`${i + 1} star`}\n          onMouseEnter={() => setHover(i + 1)} onMouseLeave={() => setHover(null)}\n          onClick={() => onChange(i + 1)}\n          style={{ border: 'none', background: 'transparent', padding: 2, cursor: 'pointer', lineHeight: 0, WebkitTapHighlightColor: 'transparent' }}\n        >{star(i)}</button>\n      ))}\n    </span>\n  );\n}\n","components/restaurant/AddressSelector.jsx":"import React from 'react';\n\n/**\n * AddressSelector — compact tappable field showing the current delivery\n * address with a pin icon and chevron, opening an address picker/sheet.\n */\nexport function AddressSelector({ label = 'Deliver to', address = 'Add an address', onClick = () => {} }) {\n  return (\n    <button type=\"button\" onClick={onClick} style={{\n      display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', width: '100%', minHeight: 48,\n      padding: '0 var(--space-md)', border: 'none', background: 'transparent', cursor: 'pointer',\n      textAlign: 'left', fontFamily: 'var(--font-family-base)', WebkitTapHighlightColor: 'transparent',\n    }}>\n      <i className=\"ph-fill ph-map-pin\" aria-hidden=\"true\" style={{ fontSize: 'var(--icon-md)', color: 'var(--color-brand-primary)' }} />\n      <span style={{ flex: 1, minWidth: 0 }}>\n        <span style={{ display: 'block', fontSize: 'var(--font-size-overline)', letterSpacing: 'var(--letter-spacing-overline)', textTransform: 'uppercase', color: 'var(--color-text-tertiary)', fontWeight: 'var(--font-weight-semibold)' }}>{label}</span>\n        <span style={{ display: 'block', fontSize: 'var(--font-size-body-lg)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{address}</span>\n      </span>\n      <i className=\"ph ph-caret-down\" aria-hidden=\"true\" style={{ fontSize: 16, color: 'var(--color-text-tertiary)' }} />\n    </button>\n  );\n}\n","components/restaurant/BranchCard.jsx":"import React from 'react';\n\n/**\n * BranchCard — single row/card for a restaurant's physical location:\n * photo, name, address and today's working hours. Used in a branch\n * picker/listing (choose pickup location, view all locations).\n */\nexport function BranchCard({\n  image = null,\n  name = '',\n  address = '',\n  hours = '',\n  isOpenNow = true,\n  distance = null,     // e.g. \"1.2 km\"\n  onClick = null,\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  const interactive = Boolean(onClick);\n  const Tag = interactive ? 'button' : 'div';\n\n  return (\n    <Tag\n      type={interactive ? 'button' : undefined} onClick={onClick}\n      onPointerDown={() => interactive && setPressed(true)}\n      onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n      style={{\n        display: 'flex', alignItems: 'center', gap: 'var(--space-md)', width: '100%',\n        padding: 'var(--space-md)', boxSizing: 'border-box', textAlign: 'left',\n        background: 'var(--card-bg)', border: 'var(--border-width-default) solid var(--card-border)',\n        borderRadius: 'var(--radius-card)', boxShadow: 'var(--elevation-card)',\n        cursor: interactive ? 'pointer' : 'default', fontFamily: 'var(--font-family-base)',\n        transform: pressed ? 'scale(var(--press-scale))' : 'none', transition: 'var(--transition-press)',\n      }}\n    >\n      <div style={{\n        flex: '0 0 auto', width: 72, height: 72, borderRadius: 'var(--radius-md)', overflow: 'hidden',\n        background: image ? `center/cover no-repeat url(\"${image}\")` : 'var(--color-bg-surface-sunken)',\n      }} />\n      <div style={{ flex: 1, minWidth: 0 }}>\n        <div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)' }}>\n          <span style={{\n            fontSize: 'var(--font-size-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)',\n            overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',\n          }}>{name}</span>\n          {distance && (\n            <span style={{ flex: '0 0 auto', fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)' }}>· {distance}</span>\n          )}\n        </div>\n        <div style={{\n          display: 'flex', alignItems: 'flex-start', gap: 6, marginTop: 4,\n          fontSize: 'var(--font-size-body)', color: 'var(--color-text-secondary)',\n        }}>\n          <i className=\"ph ph-map-pin\" aria-hidden=\"true\" style={{ fontSize: 16, marginTop: 2, flex: '0 0 auto', color: 'var(--color-text-tertiary)' }} />\n          <span style={{ overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{address}</span>\n        </div>\n        <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginTop: 4 }}>\n          <i className=\"ph ph-clock\" aria-hidden=\"true\" style={{ fontSize: 16, flex: '0 0 auto', color: 'var(--color-text-tertiary)' }} />\n          <span style={{ fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)' }}>{hours}</span>\n          <span style={{\n            marginLeft: 4, fontSize: 11, fontWeight: 'var(--font-weight-bold)', padding: '2px 8px', borderRadius: 'var(--radius-full)',\n            background: isOpenNow ? 'var(--color-bg-success)' : 'var(--color-bg-error)',\n            color: isOpenNow ? 'var(--color-text-success)' : 'var(--color-text-error)',\n          }}>{isOpenNow ? 'Open' : 'Closed'}</span>\n        </div>\n      </div>\n      {interactive && <i className=\"ph ph-caret-right\" aria-hidden=\"true\" style={{ flex: '0 0 auto', fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)' }} />}\n    </Tag>\n  );\n}\n","components/restaurant/DeliveryPickupToggle.jsx":"import React from 'react';\nimport { SegmentedControl } from '../tabs/SegmentedControl.jsx';\n\n/**\n * DeliveryPickupToggle — thin preset wrapper over SegmentedControl for the\n * ubiquitous Delivery / Pickup switch at the top of ordering screens.\n */\nexport function DeliveryPickupToggle({ value = 'delivery', onChange = () => {} }) {\n  return (\n    <SegmentedControl\n      value={value} onChange={onChange}\n      options={[\n        { label: 'Delivery', value: 'delivery', icon: 'motorcycle' },\n        { label: 'Pickup', value: 'pickup', icon: 'storefront' },\n      ]}\n    />\n  );\n}\n","components/restaurant/DeliveryTimeEstimate.jsx":"import React from 'react';\n\n/**\n * DeliveryTimeEstimate — inline pill showing the estimated delivery/pickup\n * window with a clock icon. Neutral, informational — not a status.\n */\nexport function DeliveryTimeEstimate({ estimate = '25–35 min', icon = 'clock', tone = 'neutral' }) {\n  const tones = {\n    neutral: { bg: 'var(--color-bg-surface-sunken)', color: 'var(--color-text-secondary)' },\n    brand: { bg: 'var(--color-brand-subtle)', color: 'var(--color-text-accent)' },\n  };\n  const t = tones[tone] || tones.neutral;\n  return (\n    <span style={{\n      display: 'inline-flex', alignItems: 'center', gap: 6, padding: '6px 12px', borderRadius: 'var(--radius-full)',\n      background: t.bg, color: t.color, fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-caption)',\n      fontWeight: 'var(--font-weight-semibold)',\n    }}>\n      <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 14 }} />{estimate}\n    </span>\n  );\n}\n","components/restaurant/RestaurantCard.jsx":"import React from 'react';\nimport { RatingStars } from '../rating/RatingStars.jsx';\nimport { Badge } from '../badge/Badge.jsx';\n\n/**\n * RestaurantCard — discovery card for a restaurant: photo, name, cuisine,\n * rating, delivery time/fee. Used in lists, carousels and search results.\n */\nexport function RestaurantCard({\n  image = null,\n  name = '',\n  cuisine = '',\n  rating = 0,\n  reviewCount = null,\n  deliveryTime = '',\n  deliveryFee = null,   // 0 = \"Free delivery\"; null = hide\n  closed = false,\n  promoLabel = null,    // e.g. \"20% off\"\n  onClick = null,\n}) {\n  const [pressed, setPressed] = React.useState(false);\n  return (\n    <div onClick={onClick}\n      onPointerDown={() => onClick && setPressed(true)} onPointerUp={() => setPressed(false)} onPointerLeave={() => setPressed(false)}\n      style={{\n        borderRadius: 'var(--radius-card)', overflow: 'hidden', background: 'var(--card-bg)',\n        border: 'var(--border-width-default) solid var(--card-border)', boxShadow: 'var(--elevation-card)',\n        cursor: onClick ? 'pointer' : 'default', fontFamily: 'var(--font-family-base)',\n        transform: pressed ? 'scale(var(--press-scale))' : 'none', transition: 'var(--transition-press)',\n      }}>\n      <div style={{\n        position: 'relative', height: 140, background: image ? `center/cover no-repeat url(\"${image}\")` : 'var(--color-bg-surface-sunken)',\n      }}>\n        {promoLabel && (\n          <span style={{\n            position: 'absolute', top: 10, left: 10, background: 'var(--color-brand-primary)', color: 'var(--color-text-on-brand)',\n            padding: '4px 10px', borderRadius: 'var(--radius-full)', fontSize: 11, fontWeight: 'var(--font-weight-bold)',\n          }}>{promoLabel}</span>\n        )}\n        {closed && (\n          <div style={{\n            position: 'absolute', inset: 0, background: 'var(--alpha-black-60)', display: 'flex',\n            alignItems: 'center', justifyContent: 'center', color: '#fff', fontSize: 13, fontWeight: 'var(--font-weight-semibold)',\n          }}>Closed</div>\n        )}\n      </div>\n      <div style={{ padding: 'var(--space-md) var(--space-base)' }}>\n        <div style={{ display: 'flex', justifyContent: 'space-between', gap: 8 }}>\n          <span style={{ fontSize: 'var(--font-size-h3)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{name}</span>\n          <RatingStars value={rating} size={14} readOnly />\n        </div>\n        <div style={{ marginTop: 2, fontSize: 'var(--font-size-caption)', color: 'var(--color-text-tertiary)' }}>\n          {cuisine}{reviewCount != null ? ` · ${reviewCount} reviews` : ''}\n        </div>\n        <div style={{ display: 'flex', gap: 'var(--space-md)', marginTop: 'var(--space-sm)', fontSize: 'var(--font-size-caption)', color: 'var(--color-text-secondary)' }}>\n          <span style={{ display: 'flex', alignItems: 'center', gap: 4 }}><i className=\"ph ph-clock\" aria-hidden=\"true\" />{deliveryTime}</span>\n          {deliveryFee != null && (\n            <span style={{ display: 'flex', alignItems: 'center', gap: 4 }}>\n              <i className=\"ph ph-motorcycle\" aria-hidden=\"true\" />{deliveryFee === 0 ? 'Free delivery' : `$${deliveryFee.toFixed(2)}`}\n            </span>\n          )}\n        </div>\n      </div>\n    </div>\n  );\n}\n","components/restaurant/RestaurantHeader.jsx":"import React from 'react';\nimport { RatingStars } from '../rating/RatingStars.jsx';\n\n/**\n * RestaurantHeader — full-width hero header for a restaurant's own screen:\n * cover photo with restaurant name, description and rating overlaid, kept\n * readable by a bottom-up gradient scrim.\n */\nexport function RestaurantHeader({\n  coverImage = 'https://images.unsplash.com/photo-1517248135467-4c7edcad34c4?w=900',\n  name = '',\n  description = '',\n  rating = 0,\n  reviewCount = null,\n  isOpen = true,\n  onBack = null,\n}) {\n  return (\n    <div style={{ position: 'relative', width: '100%', height: 260, overflow: 'hidden', fontFamily: 'var(--font-family-base)', background: coverImage ? `center/cover no-repeat url(\"${coverImage}\")` : 'var(--color-bg-surface-sunken)' }}>\n      {/* Bottom-up gradient scrim for legibility */}\n      <div style={{ position: 'absolute', inset: 0, background: 'linear-gradient(to top, rgba(0,0,0,0.78) 0%, rgba(0,0,0,0.45) 32%, rgba(0,0,0,0) 62%)' }} />\n\n      {/* Top bar */}\n      {onBack && (\n        <div style={{ position: 'absolute', top: 'var(--space-md)', left: 'var(--space-md)' }}>\n          <button type=\"button\" onClick={onBack} aria-label=\"Back\" style={{\n            width: 40, height: 40, borderRadius: '50%', border: 'none', background: 'var(--alpha-white-24)',\n            backdropFilter: 'blur(6px)', color: '#fff', display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer',\n          }}><i className=\"ph ph-arrow-left\" aria-hidden=\"true\" /></button>\n        </div>\n      )}\n\n      {/* Overlaid text */}\n      <div style={{ position: 'absolute', left: 0, right: 0, bottom: 0, padding: 'var(--space-base)', color: '#fff', display: 'flex', flexDirection: 'column', alignItems: 'center', textAlign: 'center' }}>\n        <h1 style={{ margin: 0, fontSize: 'var(--font-size-h1)', lineHeight: 'var(--line-height-h1)', fontWeight: 'var(--font-weight-semibold)', color: '#fff' }}>{name}</h1>\n        {description && (\n          <p style={{ margin: '6px 0 0', fontSize: 'var(--font-size-body)', lineHeight: 'var(--line-height-body)', color: 'var(--alpha-white-80, rgba(255,255,255,0.85))' }}>{description}</p>\n        )}\n        <div style={{ marginTop: 10, display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 6 }}>\n          <RatingStars value={rating} size={15} readOnly />\n          <span style={{ fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)', color: '#fff' }}>{rating.toFixed(1)}</span>\n          {reviewCount != null && (\n            <span style={{ fontSize: 'var(--font-size-body)', color: 'rgba(255,255,255,0.8)' }}>({reviewCount})</span>\n          )}\n        </div>\n      </div>\n    </div>\n  );\n}\n","components/search/SearchBar.jsx":"import React from 'react';\n\n/**\n * SearchBar — search-optimized input with leading magnifier, clear button\n * and optional live suggestions dropdown.\n */\nexport function SearchBar({\n  value = '',\n  onChange = () => {},\n  onSubmit = () => {},\n  placeholder = 'Search dishes, restaurants…',\n  suggestions = [],    // array of strings\n  onSelectSuggestion = () => {},\n  disabled = false,\n}) {\n  const [focused, setFocused] = React.useState(false);\n  const showSug = focused && value.length > 0 && suggestions.length > 0;\n\n  const bar = {\n    display: 'flex', alignItems: 'center', gap: 'var(--space-sm)',\n    height: 48, padding: '0 var(--space-base)', boxSizing: 'border-box',\n    background: 'var(--color-bg-surface-sunken)',\n    border: `var(--border-width-default) solid ${focused ? 'var(--input-border-focus)' : 'transparent'}`,\n    borderRadius: 'var(--radius-full)',\n    opacity: disabled ? 'var(--state-disabled-opacity)' : 1,\n    transition: 'var(--transition-colors)',\n  };\n\n  return (\n    <div style={{ position: 'relative', width: '100%' }}>\n      <div style={bar}>\n        <i className=\"ph ph-magnifying-glass\" aria-hidden=\"true\" style={{ fontSize: 'var(--icon-sm)', color: 'var(--color-text-tertiary)' }} />\n        <input\n          type=\"search\" value={value} placeholder={placeholder} disabled={disabled}\n          onChange={(e) => onChange(e.target.value)}\n          onKeyDown={(e) => e.key === 'Enter' && onSubmit(value)}\n          onFocus={() => setFocused(true)}\n          onBlur={() => setTimeout(() => setFocused(false), 120)}\n          style={{\n            flex: 1, minWidth: 0, border: 'none', outline: 'none', background: 'transparent',\n            color: 'var(--input-text)', fontFamily: 'var(--font-family-base)',\n            fontSize: 'var(--font-size-body)',\n          }}\n          aria-label=\"Search\"\n        />\n        {value && (\n          <button\n            type=\"button\" onClick={() => onChange('')} aria-label=\"Clear\"\n            style={{\n              width: 24, height: 24, display: 'inline-flex', alignItems: 'center',\n              justifyContent: 'center', border: 'none', background: 'var(--color-border-default)',\n              color: 'var(--color-text-secondary)', borderRadius: 'var(--radius-full)',\n              cursor: 'pointer', fontSize: 14,\n            }}\n          >\n            <i className=\"ph ph-x\" aria-hidden=\"true\" />\n          </button>\n        )}\n      </div>\n      {showSug && (\n        <div style={{\n          position: 'absolute', top: 'calc(100% + var(--space-xs))', left: 0, right: 0, zIndex: 20,\n          background: 'var(--color-bg-surface)', border: 'var(--border-width-default) solid var(--color-border-muted)',\n          borderRadius: 'var(--radius-input)', boxShadow: 'var(--elevation-overlay)', padding: 'var(--space-xs)',\n        }} role=\"listbox\">\n          {suggestions.map((s) => (\n            <button\n              key={s} type=\"button\" role=\"option\"\n              onClick={() => { onSelectSuggestion(s); onChange(s); }}\n              style={{\n                display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', width: '100%', minHeight: 44,\n                padding: '0 var(--space-md)', border: 'none', background: 'transparent',\n                borderRadius: 'var(--radius-sm)', cursor: 'pointer', textAlign: 'left',\n                fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-body)',\n                color: 'var(--color-text-primary)',\n              }}\n            >\n              <i className=\"ph ph-magnifying-glass\" aria-hidden=\"true\" style={{ fontSize: 16, color: 'var(--color-text-tertiary)' }} />\n              {s}\n            </button>\n          ))}\n        </div>\n      )}\n    </div>\n  );\n}\n","components/search/SearchFiltersPanel.jsx":"import React from 'react';\nimport { Slider } from '../forms/Slider.jsx';\nimport { Chip } from '../chip/Chip.jsx';\nimport { Button } from '../buttons/Button.jsx';\n\n/**\n * SearchFiltersPanel — grouped filter controls for search results: max\n * price, dietary tags, and sort. Composes Slider + Tag + Button; typically\n * shown inside a BottomSheet.\n */\nexport function SearchFiltersPanel({\n  maxPrice = 30,\n  onMaxPriceChange = () => {},\n  dietaryOptions = ['Vegetarian', 'Vegan', 'Gluten-free'],\n  selectedDietary = [],\n  onToggleDietary = () => {},\n  onApply = () => {},\n  onReset = () => {},\n}) {\n  return (\n    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-lg)', fontFamily: 'var(--font-family-base)' }}>\n      <div>\n        <h3 style={{ margin: '0 0 var(--space-md)', fontSize: 'var(--font-size-h3)', color: 'var(--color-text-primary)' }}>Max price</h3>\n        <Slider min={5} max={50} step={5} value={maxPrice} onChange={onMaxPriceChange} formatValue={(v) => `$${v}`} />\n      </div>\n      <div>\n        <h3 style={{ margin: '0 0 var(--space-md)', fontSize: 'var(--font-size-h3)', color: 'var(--color-text-primary)' }}>Dietary</h3>\n        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8 }}>\n          {dietaryOptions.map((d) => (\n            <Chip key={d} label={d} selected={selectedDietary.includes(d)} onClick={() => onToggleDietary(d)} />\n          ))}\n        </div>\n      </div>\n      <div style={{ display: 'flex', gap: 'var(--space-md)' }}>\n        <Button variant=\"secondary\" onClick={onReset} fullWidth>Reset</Button>\n        <Button onClick={onApply} fullWidth>Apply filters</Button>\n      </div>\n    </div>\n  );\n}\n","components/tabs/MenuCategoryTabs.jsx":"import React from 'react';\n\n/**\n * MenuCategoryTabs — sticky horizontal tab strip for jumping between\n * menu sections (Starters, Mains, Desserts…). Sliding accent underline.\n */\nexport function MenuCategoryTabs({ categories = [], activeKey = null, onChange = () => {} }) {\n  return (\n    <div role=\"tablist\" aria-label=\"Menu categories\" style={{\n      display: 'flex', gap: 'var(--space-lg)', overflowX: 'auto',\n      borderBottom: 'var(--border-width-default) solid var(--color-border-muted)',\n      fontFamily: 'var(--font-family-base)',\n    }}>\n      {categories.map((c) => {\n        const key = typeof c === 'string' ? c : c.key;\n        const label = typeof c === 'string' ? c : c.label;\n        const active = key === activeKey;\n        return (\n          <button key={key} type=\"button\" role=\"tab\" aria-selected={active} onClick={() => onChange(key)} style={{\n            flex: '0 0 auto', border: 'none', background: 'transparent', cursor: 'pointer',\n            padding: 'var(--space-md) 2px', WebkitTapHighlightColor: 'transparent',\n            fontSize: 'var(--font-size-body-lg)', fontWeight: active ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n            color: active ? 'var(--color-text-primary)' : 'var(--color-text-tertiary)',\n            borderBottom: active ? '2px solid var(--color-brand-primary)' : '2px solid transparent',\n            marginBottom: -1, transition: 'color var(--duration-base) var(--ease-standard)', whiteSpace: 'nowrap',\n          }}>{label}</button>\n        );\n      })}\n    </div>\n  );\n}\n","components/tabs/SegmentedControl.jsx":"import React from 'react';\n\n/**\n * SegmentedControl (Tab Bar) — horizontal set of tabs for switching views\n * within a single screen. Sliding accent indicator. Also used for\n * Delivery/Pickup style two-option toggles.\n */\nexport function SegmentedControl({\n  options = [],        // [{ label, value, icon? }] or strings\n  value = null,\n  onChange = () => {},\n  fullWidth = true,\n}) {\n  const norm = options.map((o) => (typeof o === 'string' ? { label: o, value: o } : o));\n  const idx = Math.max(0, norm.findIndex((o) => o.value === value));\n\n  const wrap = {\n    position: 'relative', display: 'inline-flex', width: fullWidth ? '100%' : 'auto',\n    padding: 4, boxSizing: 'border-box', background: 'var(--color-bg-surface-sunken)',\n    borderRadius: 'var(--radius-full)', fontFamily: 'var(--font-family-base)',\n  };\n  const indicator = {\n    position: 'absolute', top: 4, bottom: 4, left: 4,\n    width: `calc((100% - 8px) / ${norm.length})`,\n    transform: `translateX(${idx * 100}%)`,\n    background: 'var(--color-bg-main)', borderRadius: 'var(--radius-full)',\n    boxShadow: 'var(--elevation-card)',\n    transition: 'transform var(--duration-base) var(--ease-standard)',\n  };\n\n  return (\n    <div style={wrap} role=\"tablist\">\n      <span style={indicator} aria-hidden=\"true\" />\n      {norm.map((o) => {\n        const active = o.value === value;\n        return (\n          <button\n            key={String(o.value)} type=\"button\" role=\"tab\" aria-selected={active}\n            onClick={() => onChange(o.value)}\n            style={{\n              position: 'relative', zIndex: 1, flex: fullWidth ? 1 : '0 0 auto',\n              display: 'inline-flex', alignItems: 'center', justifyContent: 'center', gap: 6,\n              minHeight: 40, padding: '0 var(--space-base)', border: 'none', background: 'transparent',\n              color: active ? 'var(--color-text-primary)' : 'var(--color-text-tertiary)',\n              fontSize: 'var(--font-size-body)', fontWeight: active ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',\n              cursor: 'pointer', WebkitTapHighlightColor: 'transparent', whiteSpace: 'nowrap',\n              transition: 'color var(--duration-base) var(--ease-standard)',\n            }}\n          >\n            {o.icon && <i className={`ph ph-${o.icon}`} aria-hidden=\"true\" style={{ fontSize: 18 }} />}\n            {o.label}\n          </button>\n        );\n      })}\n    </div>\n  );\n}\n","components/tag/Tag.jsx":"import React from 'react';\n\n/**\n * Tag — static compact label for categories or attributes. Not\n * interactive. For a selectable/removable toggle, use Chip instead.\n */\nexport function Tag({\n  label = '',\n  icon = null,         // phosphor name\n  tone = 'neutral',    // 'neutral' | 'brand' | 'success' | 'warning' | 'error'\n  size = 'md',         // 'sm' 24 | 'md' 32\n}) {\n  const h = size === 'sm' ? 24 : 32;\n  const tones = {\n    neutral: { bg: 'var(--chip-bg)', color: 'var(--chip-text)' },\n    brand: { bg: 'var(--color-brand-subtle)', color: 'var(--color-text-accent)' },\n    success: { bg: 'var(--color-bg-success)', color: 'var(--color-text-success)' },\n    warning: { bg: 'var(--color-bg-warning)', color: 'var(--color-text-warning)' },\n    error: { bg: 'var(--color-bg-error)', color: 'var(--color-text-error)' },\n  };\n  const t = tones[tone] || tones.neutral;\n\n  return (\n    <span style={{\n      display: 'inline-flex', alignItems: 'center', gap: 6, height: h,\n      padding: `0 ${size === 'sm' ? 10 : 12}px`, boxSizing: 'border-box',\n      background: t.bg, color: t.color, border: 'none', borderRadius: 'var(--radius-full)',\n      fontFamily: 'var(--font-family-base)', fontSize: size === 'sm' ? 12 : 'var(--font-size-body)',\n      fontWeight: 'var(--font-weight-semibold)', whiteSpace: 'nowrap',\n    }}>\n      {icon && <i className={`ph ph-${icon}`} aria-hidden=\"true\" style={{ fontSize: 16 }} />}\n      {label}\n    </span>\n  );\n}\n","components/tooltip/Tooltip.jsx":"import React from 'react';\n\n/**\n * Tooltip — small contextual hint shown on tap/hover of its trigger.\n * Wraps a child trigger; positions above or below.\n */\nexport function Tooltip({ content = '', placement = 'top', children }) {\n  const [open, setOpen] = React.useState(false);\n  const above = placement === 'top';\n\n  return (\n    <span style={{ position: 'relative', display: 'inline-flex' }}\n      onMouseEnter={() => setOpen(true)} onMouseLeave={() => setOpen(false)}\n      onFocus={() => setOpen(true)} onBlur={() => setOpen(false)}>\n      {children}\n      <span role=\"tooltip\" style={{\n        position: 'absolute', left: '50%', transform: `translateX(-50%) translateY(${open ? '0' : above ? '4px' : '-4px'})`,\n        [above ? 'bottom' : 'top']: 'calc(100% + 8px)', zIndex: 80,\n        background: 'var(--tooltip-bg)', color: 'var(--tooltip-text)',\n        padding: '6px 10px', borderRadius: 'var(--radius-tooltip)', whiteSpace: 'nowrap',\n        fontFamily: 'var(--font-family-base)', fontSize: 'var(--font-size-caption)', lineHeight: 'var(--line-height-caption)',\n        boxShadow: 'var(--elevation-overlay)', pointerEvents: 'none',\n        opacity: open ? 1 : 0, transition: 'opacity var(--duration-fast) var(--ease-standard), transform var(--duration-fast) var(--ease-standard)',\n      }}>{content}</span>\n    </span>\n  );\n}\n","components/section-header/SectionHeader.jsx":"import React from 'react';\n\n/**\n * SectionHeader — Heading 2 styled label used to introduce a page section\n * (e.g. \"Popular near you\", \"Your orders\").\n */\nexport function SectionHeader({ title = '', action = null, onActionClick = null }) {\n  return (\n    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 'var(--space-md)', width: '100%', boxSizing: 'border-box', fontFamily: 'var(--font-family-base)' }}>\n      <span style={{ fontSize: 'var(--font-size-h2)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-text-primary)', lineHeight: 'var(--line-height-heading)' }}>{title}</span>\n      {action && (\n        <button type=\"button\" onClick={onActionClick} style={{ background: 'none', border: 'none', padding: 0, cursor: 'pointer', fontSize: 'var(--font-size-body)', fontWeight: 'var(--font-weight-semibold)', color: 'var(--color-brand-primary)', fontFamily: 'inherit' }}>{action}</button>\n      )}\n    </div>\n  );\n}\n"};
  const META = [{"names":["Avatar"],"path":"components/avatar/Avatar.jsx","deps":[]},{"names":["Badge"],"path":"components/badge/Badge.jsx","deps":[]},{"names":["Button"],"path":"components/buttons/Button.jsx","deps":[]},{"names":["CartSummaryBar"],"path":"components/cart/CartSummaryBar.jsx","deps":[]},{"names":["OrderTotalBreakdown"],"path":"components/cart/OrderTotalBreakdown.jsx","deps":[]},{"names":["PromoCodeInput"],"path":"components/cart/PromoCodeInput.jsx","deps":[]},{"names":["PaymentMethodCard"],"path":"components/checkout/PaymentMethodCard.jsx","deps":[]},{"names":["PaymentMethodPicker"],"path":"components/checkout/PaymentMethodPicker.jsx","deps":["PaymentMethodCard"]},{"names":["TipSelector"],"path":"components/checkout/TipSelector.jsx","deps":[]},{"names":["Chip"],"path":"components/chip/Chip.jsx","deps":[]},{"names":["ReservationConfirmationCard"],"path":"components/confirmations/ReservationConfirmationCard.jsx","deps":[]},{"names":["Card"],"path":"components/data-display/Card.jsx","deps":[]},{"names":["Divider"],"path":"components/data-display/Divider.jsx","deps":[]},{"names":["Fab"],"path":"components/fab/Fab.jsx","deps":[]},{"names":["BottomSheet"],"path":"components/feedback/BottomSheet.jsx","deps":[]},{"names":["Dialog"],"path":"components/feedback/Dialog.jsx","deps":[]},{"names":["EmptyState"],"path":"components/feedback/EmptyState.jsx","deps":[]},{"names":["ErrorState"],"path":"components/feedback/ErrorState.jsx","deps":[]},{"names":["ProgressBar"],"path":"components/feedback/ProgressBar.jsx","deps":[]},{"names":["Skeleton"],"path":"components/feedback/Skeleton.jsx","deps":[]},{"names":["Spinner"],"path":"components/feedback/Spinner.jsx","deps":[]},{"names":["Toast"],"path":"components/feedback/Toast.jsx","deps":[]},{"names":["Filter"],"path":"components/filter/Filter.jsx","deps":[]},{"names":["FilterBar"],"path":"components/filter/FilterBar.jsx","deps":[]},{"names":["Checkbox"],"path":"components/forms/Checkbox.jsx","deps":[]},{"names":["FormGroup"],"path":"components/forms/FormGroup.jsx","deps":[]},{"names":["Radio","RadioGroup"],"path":"components/forms/Radio.jsx","deps":[]},{"names":["Slider"],"path":"components/forms/Slider.jsx","deps":[]},{"names":["Stepper"],"path":"components/forms/Stepper.jsx","deps":[]},{"names":["Switch"],"path":"components/forms/Switch.jsx","deps":[]},{"names":["IconButton"],"path":"components/icon-button/IconButton.jsx","deps":[]},{"names":["Accordion"],"path":"components/inputs/Accordion.jsx","deps":[]},{"names":["Dropdown"],"path":"components/inputs/Dropdown.jsx","deps":[]},{"names":["TextArea"],"path":"components/inputs/TextArea.jsx","deps":[]},{"names":["TextInput"],"path":"components/inputs/TextInput.jsx","deps":[]},{"names":["AdvancedListItem"],"path":"components/list-items/AdvancedListItem.jsx","deps":[]},{"names":["SimpleListItem"],"path":"components/list-items/SimpleListItem.jsx","deps":[]},{"names":["LoyaltyRewardsBadge"],"path":"components/loyalty/LoyaltyRewardsBadge.jsx","deps":[]},{"names":["LoyaltyWidget"],"path":"components/loyalty/LoyaltyWidget.jsx","deps":[]},{"names":["MapView"],"path":"components/media/MapView.jsx","deps":[]},{"names":["CartLineItem"],"path":"components/menu/CartLineItem.jsx","deps":["Stepper"]},{"names":["ItemCustomizationSheet"],"path":"components/menu/ItemCustomizationSheet.jsx","deps":["BottomSheet","RadioGroup","Checkbox","Stepper","Button"]},{"names":["MenuItemCard"],"path":"components/menu/MenuItemCard.jsx","deps":["Stepper"]},{"names":["BottomTabBar"],"path":"components/navigation/BottomTabBar.jsx","deps":[]},{"names":["Breadcrumb"],"path":"components/navigation/Breadcrumb.jsx","deps":[]},{"names":["NavigationDrawer"],"path":"components/navigation/NavigationDrawer.jsx","deps":[]},{"names":["PageIndicator"],"path":"components/navigation/PageIndicator.jsx","deps":[]},{"names":["TopAppBar"],"path":"components/navigation/TopAppBar.jsx","deps":[]},{"names":["NewsListing"],"path":"components/news/NewsListing.jsx","deps":[]},{"names":["LiveOrderTrackingMap"],"path":"components/order/LiveOrderTrackingMap.jsx","deps":["MapView"]},{"names":["OrderHistoryCard"],"path":"components/order/OrderHistoryCard.jsx","deps":[]},{"names":["OrderStatusTimeline"],"path":"components/order/OrderStatusTimeline.jsx","deps":[]},{"names":["RatingReviewPrompt"],"path":"components/order/RatingReviewPrompt.jsx","deps":["RatingStars","Button"]},{"names":["ReorderButton"],"path":"components/order/ReorderButton.jsx","deps":[]},{"names":["Calendar"],"path":"components/pickers/Calendar.jsx","deps":[]},{"names":["DatePicker"],"path":"components/pickers/DatePicker.jsx","deps":[]},{"names":["TimePicker"],"path":"components/pickers/TimePicker.jsx","deps":[]},{"names":["QuickActionTile","QuickActionTiles"],"path":"components/quick-action-tiles/QuickActionTiles.jsx","deps":[]},{"names":["RatingStars"],"path":"components/rating/RatingStars.jsx","deps":[]},{"names":["AddressSelector"],"path":"components/restaurant/AddressSelector.jsx","deps":[]},{"names":["BranchCard"],"path":"components/restaurant/BranchCard.jsx","deps":[]},{"names":["DeliveryPickupToggle"],"path":"components/restaurant/DeliveryPickupToggle.jsx","deps":["SegmentedControl"]},{"names":["DeliveryTimeEstimate"],"path":"components/restaurant/DeliveryTimeEstimate.jsx","deps":[]},{"names":["RestaurantCard"],"path":"components/restaurant/RestaurantCard.jsx","deps":["RatingStars","Badge"]},{"names":["RestaurantHeader"],"path":"components/restaurant/RestaurantHeader.jsx","deps":["RatingStars","IconButton"]},{"names":["SearchBar"],"path":"components/search/SearchBar.jsx","deps":[]},{"names":["SearchFiltersPanel"],"path":"components/search/SearchFiltersPanel.jsx","deps":["Slider","Chip","Button"]},{"names":["MenuCategoryTabs"],"path":"components/tabs/MenuCategoryTabs.jsx","deps":[]},{"names":["SegmentedControl"],"path":"components/tabs/SegmentedControl.jsx","deps":[]},{"names":["Tag"],"path":"components/tag/Tag.jsx","deps":[]},{"names":["Tooltip"],"path":"components/tooltip/Tooltip.jsx","deps":[]},{"names":["SectionHeader"],"path":"components/section-header/SectionHeader.jsx","deps":[]}];

  const BABEL_URL = 'https://unpkg.com/@babel/standalone@7.29.0/babel.min.js';
  let babelLoading = null;
  function ensureBabel() {
    if (window.Babel) return Promise.resolve();
    if (babelLoading) return babelLoading;
    babelLoading = new Promise((res, rej) => {
      const s = document.createElement('script');
      s.src = BABEL_URL; s.crossOrigin = 'anonymous';
      s.onload = () => res(); s.onerror = () => rej(new Error('Failed to load Babel'));
      document.head.appendChild(s);
    });
    return babelLoading;
  }
  function stripModuleSyntax(src) {
    return src
      .replace(/^import[^\n]*\n/gm, '')
      .replace(/export\s+function/g, 'function')
      .replace(/export\s+const/g, 'const');
  }
  function loadOne(meta) {
    try {
      const raw = SOURCES[meta.path];
      const src = stripModuleSyntax(raw);
      let code = window.Babel.transform(src, { filename: meta.path, presets: ['react', 'typescript'] }).code;
      meta.deps.forEach((d) => {
        code = code.replace(new RegExp('React\\.createElement\\(' + d + '(?=[,)])', 'g'), 'React.createElement(__ds_scope.' + d);
      });
      const returnObj = '({' + meta.names.map((n) => n + ': (typeof ' + n + " !== 'undefined' ? " + n + ' : undefined)').join(', ') + '})';
      const fn = new Function('React', '__ds_scope', code + ';\nreturn ' + returnObj + ';');
      const exported = fn(window.React, __ds_scope);
      Object.assign(__ds_scope, exported);
      Object.assign(__ds_ns, exported);
    } catch (e) {
      __ds_ns.__errors.push({ path: meta.path, error: String((e && e.message) || e) });
    }
  }
  (async () => {
    await ensureBabel();
    META.forEach(loadOne);
    __ds_ns.__ready = true;
  })();
})();
